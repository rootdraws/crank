/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const UPDATE_MINT_DISCRIMINATOR = new Uint8Array([
  212, 203, 57, 78, 75, 245, 222, 5,
]);

export function getUpdateMintDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(UPDATE_MINT_DISCRIMINATOR);
}

export type UpdateMintInstruction<
  TProgram extends string = typeof MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS,
  TAccountDistributor extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountOldVault extends string | AccountMeta<string> = string,
  TAccountNewMint extends string | AccountMeta<string> = string,
  TAccountNewVault extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountDistributor extends string
        ? WritableAccount<TAccountDistributor>
        : TAccountDistributor,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountOldVault extends string
        ? ReadonlyAccount<TAccountOldVault>
        : TAccountOldVault,
      TAccountNewMint extends string
        ? ReadonlyAccount<TAccountNewMint>
        : TAccountNewMint,
      TAccountNewVault extends string
        ? ReadonlyAccount<TAccountNewVault>
        : TAccountNewVault,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateMintInstructionData = { discriminator: ReadonlyUint8Array };

export type UpdateMintInstructionDataArgs = {};

export function getUpdateMintInstructionDataEncoder(): FixedSizeEncoder<UpdateMintInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: UPDATE_MINT_DISCRIMINATOR })
  );
}

export function getUpdateMintInstructionDataDecoder(): FixedSizeDecoder<UpdateMintInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getUpdateMintInstructionDataCodec(): FixedSizeCodec<
  UpdateMintInstructionDataArgs,
  UpdateMintInstructionData
> {
  return combineCodec(
    getUpdateMintInstructionDataEncoder(),
    getUpdateMintInstructionDataDecoder()
  );
}

export type UpdateMintAsyncInput<
  TAccountDistributor extends string = string,
  TAccountAuthority extends string = string,
  TAccountOldVault extends string = string,
  TAccountNewMint extends string = string,
  TAccountNewVault extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  distributor?: Address<TAccountDistributor>;
  authority: TransactionSigner<TAccountAuthority>;
  /** Current vault — must be drained (amount == 0) before mint can be changed. */
  oldVault: Address<TAccountOldVault>;
  newMint: Address<TAccountNewMint>;
  /** New vault ATA owned by the distributor PDA, denominated in new_mint. */
  newVault: Address<TAccountNewVault>;
  tokenProgram: Address<TAccountTokenProgram>;
};

export async function getUpdateMintInstructionAsync<
  TAccountDistributor extends string,
  TAccountAuthority extends string,
  TAccountOldVault extends string,
  TAccountNewMint extends string,
  TAccountNewVault extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS,
>(
  input: UpdateMintAsyncInput<
    TAccountDistributor,
    TAccountAuthority,
    TAccountOldVault,
    TAccountNewMint,
    TAccountNewVault,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  UpdateMintInstruction<
    TProgramAddress,
    TAccountDistributor,
    TAccountAuthority,
    TAccountOldVault,
    TAccountNewMint,
    TAccountNewVault,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    distributor: { value: input.distributor ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    oldVault: { value: input.oldVault ?? null, isWritable: false },
    newMint: { value: input.newMint ?? null, isWritable: false },
    newVault: { value: input.newVault ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.distributor.value) {
    accounts.distributor.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([100, 105, 115, 116, 114, 105, 98, 117, 116, 111, 114])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.distributor),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.oldVault),
      getAccountMeta(accounts.newMint),
      getAccountMeta(accounts.newVault),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getUpdateMintInstructionDataEncoder().encode({}),
    programAddress,
  } as UpdateMintInstruction<
    TProgramAddress,
    TAccountDistributor,
    TAccountAuthority,
    TAccountOldVault,
    TAccountNewMint,
    TAccountNewVault,
    TAccountTokenProgram
  >);
}

export type UpdateMintInput<
  TAccountDistributor extends string = string,
  TAccountAuthority extends string = string,
  TAccountOldVault extends string = string,
  TAccountNewMint extends string = string,
  TAccountNewVault extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  distributor: Address<TAccountDistributor>;
  authority: TransactionSigner<TAccountAuthority>;
  /** Current vault — must be drained (amount == 0) before mint can be changed. */
  oldVault: Address<TAccountOldVault>;
  newMint: Address<TAccountNewMint>;
  /** New vault ATA owned by the distributor PDA, denominated in new_mint. */
  newVault: Address<TAccountNewVault>;
  tokenProgram: Address<TAccountTokenProgram>;
};

export function getUpdateMintInstruction<
  TAccountDistributor extends string,
  TAccountAuthority extends string,
  TAccountOldVault extends string,
  TAccountNewMint extends string,
  TAccountNewVault extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS,
>(
  input: UpdateMintInput<
    TAccountDistributor,
    TAccountAuthority,
    TAccountOldVault,
    TAccountNewMint,
    TAccountNewVault,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): UpdateMintInstruction<
  TProgramAddress,
  TAccountDistributor,
  TAccountAuthority,
  TAccountOldVault,
  TAccountNewMint,
  TAccountNewVault,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    distributor: { value: input.distributor ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    oldVault: { value: input.oldVault ?? null, isWritable: false },
    newMint: { value: input.newMint ?? null, isWritable: false },
    newVault: { value: input.newVault ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.distributor),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.oldVault),
      getAccountMeta(accounts.newMint),
      getAccountMeta(accounts.newVault),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getUpdateMintInstructionDataEncoder().encode({}),
    programAddress,
  } as UpdateMintInstruction<
    TProgramAddress,
    TAccountDistributor,
    TAccountAuthority,
    TAccountOldVault,
    TAccountNewMint,
    TAccountNewVault,
    TAccountTokenProgram
  >);
}

export type ParsedUpdateMintInstruction<
  TProgram extends string = typeof MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    distributor: TAccountMetas[0];
    authority: TAccountMetas[1];
    /** Current vault — must be drained (amount == 0) before mint can be changed. */
    oldVault: TAccountMetas[2];
    newMint: TAccountMetas[3];
    /** New vault ATA owned by the distributor PDA, denominated in new_mint. */
    newVault: TAccountMetas[4];
    tokenProgram: TAccountMetas[5];
  };
  data: UpdateMintInstructionData;
};

export function parseUpdateMintInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateMintInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      distributor: getNextAccount(),
      authority: getNextAccount(),
      oldVault: getNextAccount(),
      newMint: getNextAccount(),
      newVault: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getUpdateMintInstructionDataDecoder().decode(instruction.data),
  };
}
