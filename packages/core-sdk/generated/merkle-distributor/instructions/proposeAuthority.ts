/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const PROPOSE_AUTHORITY_DISCRIMINATOR = new Uint8Array([
  20, 148, 236, 198, 76, 119, 99, 142,
]);

export function getProposeAuthorityDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PROPOSE_AUTHORITY_DISCRIMINATOR
  );
}

export type ProposeAuthorityInstruction<
  TProgram extends string = typeof MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS,
  TAccountDistributor extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountDistributor extends string
        ? WritableAccount<TAccountDistributor>
        : TAccountDistributor,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type ProposeAuthorityInstructionData = {
  discriminator: ReadonlyUint8Array;
  newAuthority: Address;
};

export type ProposeAuthorityInstructionDataArgs = { newAuthority: Address };

export function getProposeAuthorityInstructionDataEncoder(): FixedSizeEncoder<ProposeAuthorityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['newAuthority', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: PROPOSE_AUTHORITY_DISCRIMINATOR })
  );
}

export function getProposeAuthorityInstructionDataDecoder(): FixedSizeDecoder<ProposeAuthorityInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['newAuthority', getAddressDecoder()],
  ]);
}

export function getProposeAuthorityInstructionDataCodec(): FixedSizeCodec<
  ProposeAuthorityInstructionDataArgs,
  ProposeAuthorityInstructionData
> {
  return combineCodec(
    getProposeAuthorityInstructionDataEncoder(),
    getProposeAuthorityInstructionDataDecoder()
  );
}

export type ProposeAuthorityAsyncInput<
  TAccountDistributor extends string = string,
  TAccountAuthority extends string = string,
> = {
  distributor?: Address<TAccountDistributor>;
  authority: TransactionSigner<TAccountAuthority>;
  newAuthority: ProposeAuthorityInstructionDataArgs['newAuthority'];
};

export async function getProposeAuthorityInstructionAsync<
  TAccountDistributor extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS,
>(
  input: ProposeAuthorityAsyncInput<TAccountDistributor, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ProposeAuthorityInstruction<
    TProgramAddress,
    TAccountDistributor,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    distributor: { value: input.distributor ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.distributor.value) {
    accounts.distributor.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([100, 105, 115, 116, 114, 105, 98, 117, 116, 111, 114])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.distributor),
      getAccountMeta(accounts.authority),
    ],
    data: getProposeAuthorityInstructionDataEncoder().encode(
      args as ProposeAuthorityInstructionDataArgs
    ),
    programAddress,
  } as ProposeAuthorityInstruction<
    TProgramAddress,
    TAccountDistributor,
    TAccountAuthority
  >);
}

export type ProposeAuthorityInput<
  TAccountDistributor extends string = string,
  TAccountAuthority extends string = string,
> = {
  distributor: Address<TAccountDistributor>;
  authority: TransactionSigner<TAccountAuthority>;
  newAuthority: ProposeAuthorityInstructionDataArgs['newAuthority'];
};

export function getProposeAuthorityInstruction<
  TAccountDistributor extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS,
>(
  input: ProposeAuthorityInput<TAccountDistributor, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress }
): ProposeAuthorityInstruction<
  TProgramAddress,
  TAccountDistributor,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    distributor: { value: input.distributor ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.distributor),
      getAccountMeta(accounts.authority),
    ],
    data: getProposeAuthorityInstructionDataEncoder().encode(
      args as ProposeAuthorityInstructionDataArgs
    ),
    programAddress,
  } as ProposeAuthorityInstruction<
    TProgramAddress,
    TAccountDistributor,
    TAccountAuthority
  >);
}

export type ParsedProposeAuthorityInstruction<
  TProgram extends string = typeof MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    distributor: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: ProposeAuthorityInstructionData;
};

export function parseProposeAuthorityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedProposeAuthorityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { distributor: getNextAccount(), authority: getNextAccount() },
    data: getProposeAuthorityInstructionDataDecoder().decode(instruction.data),
  };
}
