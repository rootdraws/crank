/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';

export const DISTRIBUTOR_DISCRIMINATOR = new Uint8Array([
  90, 90, 217, 147, 6, 32, 135, 4,
]);

export function getDistributorDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DISTRIBUTOR_DISCRIMINATOR);
}

export type Distributor = {
  discriminator: ReadonlyUint8Array;
  authority: Address;
  pendingAuthority: Address;
  mint: Address;
  vault: Address;
  currentEpoch: bigint;
  merkleRoot: Array<number>;
  totalAmountFunded: bigint;
  totalAmountClaimed: bigint;
  paused: boolean;
  bump: number;
  ipfsCid: string;
};

export type DistributorArgs = {
  authority: Address;
  pendingAuthority: Address;
  mint: Address;
  vault: Address;
  currentEpoch: number | bigint;
  merkleRoot: Array<number>;
  totalAmountFunded: number | bigint;
  totalAmountClaimed: number | bigint;
  paused: boolean;
  bump: number;
  ipfsCid: string;
};

export function getDistributorEncoder(): Encoder<DistributorArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['authority', getAddressEncoder()],
      ['pendingAuthority', getAddressEncoder()],
      ['mint', getAddressEncoder()],
      ['vault', getAddressEncoder()],
      ['currentEpoch', getU64Encoder()],
      ['merkleRoot', getArrayEncoder(getU8Encoder(), { size: 32 })],
      ['totalAmountFunded', getU64Encoder()],
      ['totalAmountClaimed', getU64Encoder()],
      ['paused', getBooleanEncoder()],
      ['bump', getU8Encoder()],
      ['ipfsCid', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: DISTRIBUTOR_DISCRIMINATOR })
  );
}

export function getDistributorDecoder(): Decoder<Distributor> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['authority', getAddressDecoder()],
    ['pendingAuthority', getAddressDecoder()],
    ['mint', getAddressDecoder()],
    ['vault', getAddressDecoder()],
    ['currentEpoch', getU64Decoder()],
    ['merkleRoot', getArrayDecoder(getU8Decoder(), { size: 32 })],
    ['totalAmountFunded', getU64Decoder()],
    ['totalAmountClaimed', getU64Decoder()],
    ['paused', getBooleanDecoder()],
    ['bump', getU8Decoder()],
    ['ipfsCid', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getDistributorCodec(): Codec<DistributorArgs, Distributor> {
  return combineCodec(getDistributorEncoder(), getDistributorDecoder());
}

export function decodeDistributor<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<Distributor, TAddress>;
export function decodeDistributor<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<Distributor, TAddress>;
export function decodeDistributor<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<Distributor, TAddress> | MaybeAccount<Distributor, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getDistributorDecoder()
  );
}

export async function fetchDistributor<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<Distributor, TAddress>> {
  const maybeAccount = await fetchMaybeDistributor(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeDistributor<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<Distributor, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeDistributor(maybeAccount);
}

export async function fetchAllDistributor(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<Distributor>[]> {
  const maybeAccounts = await fetchAllMaybeDistributor(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeDistributor(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<Distributor>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeDistributor(maybeAccount));
}
