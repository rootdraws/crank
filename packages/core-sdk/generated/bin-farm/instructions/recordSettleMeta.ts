/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { BIN_FARM_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const RECORD_SETTLE_META_DISCRIMINATOR = new Uint8Array([
  175, 155, 196, 107, 102, 138, 77, 242,
]);

export function getRecordSettleMetaDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RECORD_SETTLE_META_DISCRIMINATOR
  );
}

export type RecordSettleMetaInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountMeteoraPosition extends string | AccountMeta<string> = string,
  TAccountPosition extends string | AccountMeta<string> = string,
  TAccountUserVault extends string | AccountMeta<string> = string,
  TAccountPositionSettle extends string | AccountMeta<string> = string,
  TAccountTokenXMint extends string | AccountMeta<string> = string,
  TAccountTokenYMint extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountMeteoraPosition extends string
        ? ReadonlyAccount<TAccountMeteoraPosition>
        : TAccountMeteoraPosition,
      TAccountPosition extends string
        ? ReadonlyAccount<TAccountPosition>
        : TAccountPosition,
      TAccountUserVault extends string
        ? ReadonlyAccount<TAccountUserVault>
        : TAccountUserVault,
      TAccountPositionSettle extends string
        ? WritableAccount<TAccountPositionSettle>
        : TAccountPositionSettle,
      TAccountTokenXMint extends string
        ? ReadonlyAccount<TAccountTokenXMint>
        : TAccountTokenXMint,
      TAccountTokenYMint extends string
        ? ReadonlyAccount<TAccountTokenYMint>
        : TAccountTokenYMint,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RecordSettleMetaInstructionData = {
  discriminator: ReadonlyUint8Array;
  proposer: Address;
};

export type RecordSettleMetaInstructionDataArgs = { proposer: Address };

export function getRecordSettleMetaInstructionDataEncoder(): FixedSizeEncoder<RecordSettleMetaInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['proposer', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: RECORD_SETTLE_META_DISCRIMINATOR })
  );
}

export function getRecordSettleMetaInstructionDataDecoder(): FixedSizeDecoder<RecordSettleMetaInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['proposer', getAddressDecoder()],
  ]);
}

export function getRecordSettleMetaInstructionDataCodec(): FixedSizeCodec<
  RecordSettleMetaInstructionDataArgs,
  RecordSettleMetaInstructionData
> {
  return combineCodec(
    getRecordSettleMetaInstructionDataEncoder(),
    getRecordSettleMetaInstructionDataDecoder()
  );
}

export type RecordSettleMetaAsyncInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountMeteoraPosition extends string = string,
  TAccountPosition extends string = string,
  TAccountUserVault extends string = string,
  TAccountPositionSettle extends string = string,
  TAccountTokenXMint extends string = string,
  TAccountTokenYMint extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Bot or vault.owner — same dual-caller pattern as user_close. */
  caller: TransactionSigner<TAccountCaller>;
  config?: Address<TAccountConfig>;
  meteoraPosition: Address<TAccountMeteoraPosition>;
  position: Address<TAccountPosition>;
  userVault: Address<TAccountUserVault>;
  positionSettle?: Address<TAccountPositionSettle>;
  /**
   * validator off-chain ensures the values are correct. Worst case for a
   * dishonest bot: settle_proposer at close fails or returns 0 payout.
   * No drain risk because the SPL Token transfer enforces ATA ownership.
   */
  tokenXMint: Address<TAccountTokenXMint>;
  tokenYMint: Address<TAccountTokenYMint>;
  systemProgram?: Address<TAccountSystemProgram>;
  proposer: RecordSettleMetaInstructionDataArgs['proposer'];
};

export async function getRecordSettleMetaInstructionAsync<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountMeteoraPosition extends string,
  TAccountPosition extends string,
  TAccountUserVault extends string,
  TAccountPositionSettle extends string,
  TAccountTokenXMint extends string,
  TAccountTokenYMint extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: RecordSettleMetaAsyncInput<
    TAccountCaller,
    TAccountConfig,
    TAccountMeteoraPosition,
    TAccountPosition,
    TAccountUserVault,
    TAccountPositionSettle,
    TAccountTokenXMint,
    TAccountTokenYMint,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  RecordSettleMetaInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountMeteoraPosition,
    TAccountPosition,
    TAccountUserVault,
    TAccountPositionSettle,
    TAccountTokenXMint,
    TAccountTokenYMint,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    meteoraPosition: {
      value: input.meteoraPosition ?? null,
      isWritable: false,
    },
    position: { value: input.position ?? null, isWritable: false },
    userVault: { value: input.userVault ?? null, isWritable: false },
    positionSettle: { value: input.positionSettle ?? null, isWritable: true },
    tokenXMint: { value: input.tokenXMint ?? null, isWritable: false },
    tokenYMint: { value: input.tokenYMint ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }
  if (!accounts.positionSettle.value) {
    accounts.positionSettle.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([112, 111, 115, 95, 115, 101, 116, 116, 108, 101])
        ),
        getAddressEncoder().encode(
          expectAddress(accounts.meteoraPosition.value)
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.meteoraPosition),
      getAccountMeta(accounts.position),
      getAccountMeta(accounts.userVault),
      getAccountMeta(accounts.positionSettle),
      getAccountMeta(accounts.tokenXMint),
      getAccountMeta(accounts.tokenYMint),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRecordSettleMetaInstructionDataEncoder().encode(
      args as RecordSettleMetaInstructionDataArgs
    ),
    programAddress,
  } as RecordSettleMetaInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountMeteoraPosition,
    TAccountPosition,
    TAccountUserVault,
    TAccountPositionSettle,
    TAccountTokenXMint,
    TAccountTokenYMint,
    TAccountSystemProgram
  >);
}

export type RecordSettleMetaInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountMeteoraPosition extends string = string,
  TAccountPosition extends string = string,
  TAccountUserVault extends string = string,
  TAccountPositionSettle extends string = string,
  TAccountTokenXMint extends string = string,
  TAccountTokenYMint extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Bot or vault.owner — same dual-caller pattern as user_close. */
  caller: TransactionSigner<TAccountCaller>;
  config: Address<TAccountConfig>;
  meteoraPosition: Address<TAccountMeteoraPosition>;
  position: Address<TAccountPosition>;
  userVault: Address<TAccountUserVault>;
  positionSettle: Address<TAccountPositionSettle>;
  /**
   * validator off-chain ensures the values are correct. Worst case for a
   * dishonest bot: settle_proposer at close fails or returns 0 payout.
   * No drain risk because the SPL Token transfer enforces ATA ownership.
   */
  tokenXMint: Address<TAccountTokenXMint>;
  tokenYMint: Address<TAccountTokenYMint>;
  systemProgram?: Address<TAccountSystemProgram>;
  proposer: RecordSettleMetaInstructionDataArgs['proposer'];
};

export function getRecordSettleMetaInstruction<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountMeteoraPosition extends string,
  TAccountPosition extends string,
  TAccountUserVault extends string,
  TAccountPositionSettle extends string,
  TAccountTokenXMint extends string,
  TAccountTokenYMint extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: RecordSettleMetaInput<
    TAccountCaller,
    TAccountConfig,
    TAccountMeteoraPosition,
    TAccountPosition,
    TAccountUserVault,
    TAccountPositionSettle,
    TAccountTokenXMint,
    TAccountTokenYMint,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): RecordSettleMetaInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountConfig,
  TAccountMeteoraPosition,
  TAccountPosition,
  TAccountUserVault,
  TAccountPositionSettle,
  TAccountTokenXMint,
  TAccountTokenYMint,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    meteoraPosition: {
      value: input.meteoraPosition ?? null,
      isWritable: false,
    },
    position: { value: input.position ?? null, isWritable: false },
    userVault: { value: input.userVault ?? null, isWritable: false },
    positionSettle: { value: input.positionSettle ?? null, isWritable: true },
    tokenXMint: { value: input.tokenXMint ?? null, isWritable: false },
    tokenYMint: { value: input.tokenYMint ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.meteoraPosition),
      getAccountMeta(accounts.position),
      getAccountMeta(accounts.userVault),
      getAccountMeta(accounts.positionSettle),
      getAccountMeta(accounts.tokenXMint),
      getAccountMeta(accounts.tokenYMint),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRecordSettleMetaInstructionDataEncoder().encode(
      args as RecordSettleMetaInstructionDataArgs
    ),
    programAddress,
  } as RecordSettleMetaInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountMeteoraPosition,
    TAccountPosition,
    TAccountUserVault,
    TAccountPositionSettle,
    TAccountTokenXMint,
    TAccountTokenYMint,
    TAccountSystemProgram
  >);
}

export type ParsedRecordSettleMetaInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Bot or vault.owner — same dual-caller pattern as user_close. */
    caller: TAccountMetas[0];
    config: TAccountMetas[1];
    meteoraPosition: TAccountMetas[2];
    position: TAccountMetas[3];
    userVault: TAccountMetas[4];
    positionSettle: TAccountMetas[5];
    /**
     * validator off-chain ensures the values are correct. Worst case for a
     * dishonest bot: settle_proposer at close fails or returns 0 payout.
     * No drain risk because the SPL Token transfer enforces ATA ownership.
     */
    tokenXMint: TAccountMetas[6];
    tokenYMint: TAccountMetas[7];
    systemProgram: TAccountMetas[8];
  };
  data: RecordSettleMetaInstructionData;
};

export function parseRecordSettleMetaInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedRecordSettleMetaInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      config: getNextAccount(),
      meteoraPosition: getNextAccount(),
      position: getNextAccount(),
      userVault: getNextAccount(),
      positionSettle: getNextAccount(),
      tokenXMint: getNextAccount(),
      tokenYMint: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRecordSettleMetaInstructionDataDecoder().decode(instruction.data),
  };
}
