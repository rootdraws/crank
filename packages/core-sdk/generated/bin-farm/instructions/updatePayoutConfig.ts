/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { BIN_FARM_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const UPDATE_PAYOUT_CONFIG_DISCRIMINATOR = new Uint8Array([
  138, 7, 27, 84, 247, 253, 152, 38,
]);

export function getUpdatePayoutConfigDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_PAYOUT_CONFIG_DISCRIMINATOR
  );
}

export type UpdatePayoutConfigInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountPayoutAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayoutAdmin extends string
        ? ReadonlySignerAccount<TAccountPayoutAdmin> &
            AccountSignerMeta<TAccountPayoutAdmin>
        : TAccountPayoutAdmin,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type UpdatePayoutConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
  payoutBps: number;
  matchRatioBps: number;
};

export type UpdatePayoutConfigInstructionDataArgs = {
  payoutBps: number;
  matchRatioBps: number;
};

export function getUpdatePayoutConfigInstructionDataEncoder(): FixedSizeEncoder<UpdatePayoutConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['payoutBps', getU16Encoder()],
      ['matchRatioBps', getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_PAYOUT_CONFIG_DISCRIMINATOR })
  );
}

export function getUpdatePayoutConfigInstructionDataDecoder(): FixedSizeDecoder<UpdatePayoutConfigInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['payoutBps', getU16Decoder()],
    ['matchRatioBps', getU16Decoder()],
  ]);
}

export function getUpdatePayoutConfigInstructionDataCodec(): FixedSizeCodec<
  UpdatePayoutConfigInstructionDataArgs,
  UpdatePayoutConfigInstructionData
> {
  return combineCodec(
    getUpdatePayoutConfigInstructionDataEncoder(),
    getUpdatePayoutConfigInstructionDataDecoder()
  );
}

export type UpdatePayoutConfigAsyncInput<
  TAccountPayoutAdmin extends string = string,
  TAccountConfig extends string = string,
> = {
  /**
   * payout_admin — initially Config.authority (FFwq), transferred to
   * Native Treasury PDA at realm bootstrap. Post-transfer, only
   * governance-driven invocations satisfy this signer constraint.
   */
  payoutAdmin: TransactionSigner<TAccountPayoutAdmin>;
  config?: Address<TAccountConfig>;
  payoutBps: UpdatePayoutConfigInstructionDataArgs['payoutBps'];
  matchRatioBps: UpdatePayoutConfigInstructionDataArgs['matchRatioBps'];
};

export async function getUpdatePayoutConfigInstructionAsync<
  TAccountPayoutAdmin extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: UpdatePayoutConfigAsyncInput<TAccountPayoutAdmin, TAccountConfig>,
  config?: { programAddress?: TProgramAddress }
): Promise<
  UpdatePayoutConfigInstruction<
    TProgramAddress,
    TAccountPayoutAdmin,
    TAccountConfig
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payoutAdmin: { value: input.payoutAdmin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payoutAdmin),
      getAccountMeta(accounts.config),
    ],
    data: getUpdatePayoutConfigInstructionDataEncoder().encode(
      args as UpdatePayoutConfigInstructionDataArgs
    ),
    programAddress,
  } as UpdatePayoutConfigInstruction<
    TProgramAddress,
    TAccountPayoutAdmin,
    TAccountConfig
  >);
}

export type UpdatePayoutConfigInput<
  TAccountPayoutAdmin extends string = string,
  TAccountConfig extends string = string,
> = {
  /**
   * payout_admin — initially Config.authority (FFwq), transferred to
   * Native Treasury PDA at realm bootstrap. Post-transfer, only
   * governance-driven invocations satisfy this signer constraint.
   */
  payoutAdmin: TransactionSigner<TAccountPayoutAdmin>;
  config: Address<TAccountConfig>;
  payoutBps: UpdatePayoutConfigInstructionDataArgs['payoutBps'];
  matchRatioBps: UpdatePayoutConfigInstructionDataArgs['matchRatioBps'];
};

export function getUpdatePayoutConfigInstruction<
  TAccountPayoutAdmin extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: UpdatePayoutConfigInput<TAccountPayoutAdmin, TAccountConfig>,
  config?: { programAddress?: TProgramAddress }
): UpdatePayoutConfigInstruction<
  TProgramAddress,
  TAccountPayoutAdmin,
  TAccountConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payoutAdmin: { value: input.payoutAdmin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payoutAdmin),
      getAccountMeta(accounts.config),
    ],
    data: getUpdatePayoutConfigInstructionDataEncoder().encode(
      args as UpdatePayoutConfigInstructionDataArgs
    ),
    programAddress,
  } as UpdatePayoutConfigInstruction<
    TProgramAddress,
    TAccountPayoutAdmin,
    TAccountConfig
  >);
}

export type ParsedUpdatePayoutConfigInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * payout_admin — initially Config.authority (FFwq), transferred to
     * Native Treasury PDA at realm bootstrap. Post-transfer, only
     * governance-driven invocations satisfy this signer constraint.
     */
    payoutAdmin: TAccountMetas[0];
    config: TAccountMetas[1];
  };
  data: UpdatePayoutConfigInstructionData;
};

export function parseUpdatePayoutConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdatePayoutConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { payoutAdmin: getNextAccount(), config: getNextAccount() },
    data: getUpdatePayoutConfigInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
