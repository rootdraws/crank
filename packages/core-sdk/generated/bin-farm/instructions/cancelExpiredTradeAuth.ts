/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { BIN_FARM_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const CANCEL_EXPIRED_TRADE_AUTH_DISCRIMINATOR = new Uint8Array([
  59, 238, 91, 136, 23, 130, 80, 48,
]);

export function getCancelExpiredTradeAuthDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANCEL_EXPIRED_TRADE_AUTH_DISCRIMINATOR
  );
}

export type CancelExpiredTradeAuthInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountBot extends string | AccountMeta<string> = string,
  TAccountTradeAuth extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountBot extends string
        ? WritableSignerAccount<TAccountBot> & AccountSignerMeta<TAccountBot>
        : TAccountBot,
      TAccountTradeAuth extends string
        ? WritableAccount<TAccountTradeAuth>
        : TAccountTradeAuth,
      ...TRemainingAccounts,
    ]
  >;

export type CancelExpiredTradeAuthInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CancelExpiredTradeAuthInstructionDataArgs = {};

export function getCancelExpiredTradeAuthInstructionDataEncoder(): FixedSizeEncoder<CancelExpiredTradeAuthInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CANCEL_EXPIRED_TRADE_AUTH_DISCRIMINATOR,
    })
  );
}

export function getCancelExpiredTradeAuthInstructionDataDecoder(): FixedSizeDecoder<CancelExpiredTradeAuthInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCancelExpiredTradeAuthInstructionDataCodec(): FixedSizeCodec<
  CancelExpiredTradeAuthInstructionDataArgs,
  CancelExpiredTradeAuthInstructionData
> {
  return combineCodec(
    getCancelExpiredTradeAuthInstructionDataEncoder(),
    getCancelExpiredTradeAuthInstructionDataDecoder()
  );
}

export type CancelExpiredTradeAuthInput<
  TAccountBot extends string = string,
  TAccountTradeAuth extends string = string,
> = {
  bot: TransactionSigner<TAccountBot>;
  tradeAuth: Address<TAccountTradeAuth>;
};

export function getCancelExpiredTradeAuthInstruction<
  TAccountBot extends string,
  TAccountTradeAuth extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: CancelExpiredTradeAuthInput<TAccountBot, TAccountTradeAuth>,
  config?: { programAddress?: TProgramAddress }
): CancelExpiredTradeAuthInstruction<
  TProgramAddress,
  TAccountBot,
  TAccountTradeAuth
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    bot: { value: input.bot ?? null, isWritable: true },
    tradeAuth: { value: input.tradeAuth ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.bot),
      getAccountMeta(accounts.tradeAuth),
    ],
    data: getCancelExpiredTradeAuthInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelExpiredTradeAuthInstruction<
    TProgramAddress,
    TAccountBot,
    TAccountTradeAuth
  >);
}

export type ParsedCancelExpiredTradeAuthInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    bot: TAccountMetas[0];
    tradeAuth: TAccountMetas[1];
  };
  data: CancelExpiredTradeAuthInstructionData;
};

export function parseCancelExpiredTradeAuthInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCancelExpiredTradeAuthInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { bot: getNextAccount(), tradeAuth: getNextAccount() },
    data: getCancelExpiredTradeAuthInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
