/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { BIN_FARM_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const WRAP_SOL_IN_VAULT_DISCRIMINATOR = new Uint8Array([
  178, 209, 149, 140, 92, 202, 99, 167,
]);

export function getWrapSolInVaultDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    WRAP_SOL_IN_VAULT_DISCRIMINATOR
  );
}

export type WrapSolInVaultInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountUserVault extends string | AccountMeta<string> = string,
  TAccountVaultWsolAta extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountUserVault extends string
        ? WritableAccount<TAccountUserVault>
        : TAccountUserVault,
      TAccountVaultWsolAta extends string
        ? WritableAccount<TAccountVaultWsolAta>
        : TAccountVaultWsolAta,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type WrapSolInVaultInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type WrapSolInVaultInstructionDataArgs = { amount: number | bigint };

export function getWrapSolInVaultInstructionDataEncoder(): FixedSizeEncoder<WrapSolInVaultInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['amount', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: WRAP_SOL_IN_VAULT_DISCRIMINATOR })
  );
}

export function getWrapSolInVaultInstructionDataDecoder(): FixedSizeDecoder<WrapSolInVaultInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['amount', getU64Decoder()],
  ]);
}

export function getWrapSolInVaultInstructionDataCodec(): FixedSizeCodec<
  WrapSolInVaultInstructionDataArgs,
  WrapSolInVaultInstructionData
> {
  return combineCodec(
    getWrapSolInVaultInstructionDataEncoder(),
    getWrapSolInVaultInstructionDataDecoder()
  );
}

export type WrapSolInVaultAsyncInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountUserVault extends string = string,
  TAccountVaultWsolAta extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  caller: TransactionSigner<TAccountCaller>;
  config?: Address<TAccountConfig>;
  userVault: Address<TAccountUserVault>;
  /** Lamport credit is validated by sync_native CPI. */
  vaultWsolAta: Address<TAccountVaultWsolAta>;
  tokenProgram: Address<TAccountTokenProgram>;
  amount: WrapSolInVaultInstructionDataArgs['amount'];
};

export async function getWrapSolInVaultInstructionAsync<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountUserVault extends string,
  TAccountVaultWsolAta extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: WrapSolInVaultAsyncInput<
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountVaultWsolAta,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  WrapSolInVaultInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountVaultWsolAta,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    userVault: { value: input.userVault ?? null, isWritable: true },
    vaultWsolAta: { value: input.vaultWsolAta ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.userVault),
      getAccountMeta(accounts.vaultWsolAta),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getWrapSolInVaultInstructionDataEncoder().encode(
      args as WrapSolInVaultInstructionDataArgs
    ),
    programAddress,
  } as WrapSolInVaultInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountVaultWsolAta,
    TAccountTokenProgram
  >);
}

export type WrapSolInVaultInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountUserVault extends string = string,
  TAccountVaultWsolAta extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  caller: TransactionSigner<TAccountCaller>;
  config: Address<TAccountConfig>;
  userVault: Address<TAccountUserVault>;
  /** Lamport credit is validated by sync_native CPI. */
  vaultWsolAta: Address<TAccountVaultWsolAta>;
  tokenProgram: Address<TAccountTokenProgram>;
  amount: WrapSolInVaultInstructionDataArgs['amount'];
};

export function getWrapSolInVaultInstruction<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountUserVault extends string,
  TAccountVaultWsolAta extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: WrapSolInVaultInput<
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountVaultWsolAta,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): WrapSolInVaultInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountConfig,
  TAccountUserVault,
  TAccountVaultWsolAta,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    userVault: { value: input.userVault ?? null, isWritable: true },
    vaultWsolAta: { value: input.vaultWsolAta ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.userVault),
      getAccountMeta(accounts.vaultWsolAta),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getWrapSolInVaultInstructionDataEncoder().encode(
      args as WrapSolInVaultInstructionDataArgs
    ),
    programAddress,
  } as WrapSolInVaultInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountVaultWsolAta,
    TAccountTokenProgram
  >);
}

export type ParsedWrapSolInVaultInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    caller: TAccountMetas[0];
    config: TAccountMetas[1];
    userVault: TAccountMetas[2];
    /** Lamport credit is validated by sync_native CPI. */
    vaultWsolAta: TAccountMetas[3];
    tokenProgram: TAccountMetas[4];
  };
  data: WrapSolInVaultInstructionData;
};

export function parseWrapSolInVaultInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedWrapSolInVaultInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      config: getNextAccount(),
      userVault: getNextAccount(),
      vaultWsolAta: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getWrapSolInVaultInstructionDataDecoder().decode(instruction.data),
  };
}
