/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { BIN_FARM_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const DRAIN_TREASURY_NATIVE_TO_NTP_DISCRIMINATOR = new Uint8Array([
  144, 47, 152, 42, 52, 57, 25, 38,
]);

export function getDrainTreasuryNativeToNtpDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DRAIN_TREASURY_NATIVE_TO_NTP_DISCRIMINATOR
  );
}

export type DrainTreasuryNativeToNtpInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountUserVault extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountUserVault extends string
        ? WritableAccount<TAccountUserVault>
        : TAccountUserVault,
      ...TRemainingAccounts,
    ]
  >;

export type DrainTreasuryNativeToNtpInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type DrainTreasuryNativeToNtpInstructionDataArgs = {};

export function getDrainTreasuryNativeToNtpInstructionDataEncoder(): FixedSizeEncoder<DrainTreasuryNativeToNtpInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: DRAIN_TREASURY_NATIVE_TO_NTP_DISCRIMINATOR,
    })
  );
}

export function getDrainTreasuryNativeToNtpInstructionDataDecoder(): FixedSizeDecoder<DrainTreasuryNativeToNtpInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getDrainTreasuryNativeToNtpInstructionDataCodec(): FixedSizeCodec<
  DrainTreasuryNativeToNtpInstructionDataArgs,
  DrainTreasuryNativeToNtpInstructionData
> {
  return combineCodec(
    getDrainTreasuryNativeToNtpInstructionDataEncoder(),
    getDrainTreasuryNativeToNtpInstructionDataDecoder()
  );
}

export type DrainTreasuryNativeToNtpInput<
  TAccountCaller extends string = string,
  TAccountUserVault extends string = string,
> = {
  /**
   * Must equal `user_vault.owner` (NTP for the treasury vault). Signable
   * only via SPL Governance executeTransaction; the proposal-whitelist-addin
   * must list this ix on the registrar whitelist for the proposal to pass.
   */
  caller: TransactionSigner<TAccountCaller>;
  userVault: Address<TAccountUserVault>;
};

export function getDrainTreasuryNativeToNtpInstruction<
  TAccountCaller extends string,
  TAccountUserVault extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: DrainTreasuryNativeToNtpInput<TAccountCaller, TAccountUserVault>,
  config?: { programAddress?: TProgramAddress }
): DrainTreasuryNativeToNtpInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountUserVault
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    userVault: { value: input.userVault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.userVault),
    ],
    data: getDrainTreasuryNativeToNtpInstructionDataEncoder().encode({}),
    programAddress,
  } as DrainTreasuryNativeToNtpInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountUserVault
  >);
}

export type ParsedDrainTreasuryNativeToNtpInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Must equal `user_vault.owner` (NTP for the treasury vault). Signable
     * only via SPL Governance executeTransaction; the proposal-whitelist-addin
     * must list this ix on the registrar whitelist for the proposal to pass.
     */
    caller: TAccountMetas[0];
    userVault: TAccountMetas[1];
  };
  data: DrainTreasuryNativeToNtpInstructionData;
};

export function parseDrainTreasuryNativeToNtpInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedDrainTreasuryNativeToNtpInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { caller: getNextAccount(), userVault: getNextAccount() },
    data: getDrainTreasuryNativeToNtpInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
