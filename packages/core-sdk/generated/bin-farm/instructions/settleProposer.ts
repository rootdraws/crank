/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { BIN_FARM_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const SETTLE_PROPOSER_DISCRIMINATOR = new Uint8Array([
  7, 106, 17, 75, 224, 170, 157, 11,
]);

export function getSettleProposerDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SETTLE_PROPOSER_DISCRIMINATOR
  );
}

export type SettleProposerInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountUserVault extends string | AccountMeta<string> = string,
  TAccountPositionSettle extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountMeteoraPosition extends string | AccountMeta<string> = string,
  TAccountPositionVaultOutputAta extends string | AccountMeta<string> = string,
  TAccountProposerOutputAta extends string | AccountMeta<string> = string,
  TAccountTaxReserveOutputAta extends string | AccountMeta<string> = string,
  TAccountOutputMint extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountUserVault extends string
        ? ReadonlyAccount<TAccountUserVault>
        : TAccountUserVault,
      TAccountPositionSettle extends string
        ? WritableAccount<TAccountPositionSettle>
        : TAccountPositionSettle,
      TAccountVault extends string
        ? ReadonlyAccount<TAccountVault>
        : TAccountVault,
      TAccountMeteoraPosition extends string
        ? ReadonlyAccount<TAccountMeteoraPosition>
        : TAccountMeteoraPosition,
      TAccountPositionVaultOutputAta extends string
        ? WritableAccount<TAccountPositionVaultOutputAta>
        : TAccountPositionVaultOutputAta,
      TAccountProposerOutputAta extends string
        ? WritableAccount<TAccountProposerOutputAta>
        : TAccountProposerOutputAta,
      TAccountTaxReserveOutputAta extends string
        ? WritableAccount<TAccountTaxReserveOutputAta>
        : TAccountTaxReserveOutputAta,
      TAccountOutputMint extends string
        ? ReadonlyAccount<TAccountOutputMint>
        : TAccountOutputMint,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SettleProposerInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type SettleProposerInstructionDataArgs = {};

export function getSettleProposerInstructionDataEncoder(): FixedSizeEncoder<SettleProposerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: SETTLE_PROPOSER_DISCRIMINATOR })
  );
}

export function getSettleProposerInstructionDataDecoder(): FixedSizeDecoder<SettleProposerInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getSettleProposerInstructionDataCodec(): FixedSizeCodec<
  SettleProposerInstructionDataArgs,
  SettleProposerInstructionData
> {
  return combineCodec(
    getSettleProposerInstructionDataEncoder(),
    getSettleProposerInstructionDataDecoder()
  );
}

export type SettleProposerAsyncInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountUserVault extends string = string,
  TAccountPositionSettle extends string = string,
  TAccountVault extends string = string,
  TAccountMeteoraPosition extends string = string,
  TAccountPositionVaultOutputAta extends string = string,
  TAccountProposerOutputAta extends string = string,
  TAccountTaxReserveOutputAta extends string = string,
  TAccountOutputMint extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /** Bot or vault.owner — dual-caller, mirrors user_close. */
  caller: TransactionSigner<TAccountCaller>;
  config?: Address<TAccountConfig>;
  /**
   * Treasury user_vault — vault.owner check is implicit via PDA seeding,
   * authorization checked in handler against caller.
   */
  userVault: Address<TAccountUserVault>;
  positionSettle?: Address<TAccountPositionSettle>;
  /** Per-position vault PDA — signs the SPL Token transfer to proposer. */
  vault?: Address<TAccountVault>;
  meteoraPosition: Address<TAccountMeteoraPosition>;
  /** Position vault's ATA for the output mint (tokens transferred FROM here). */
  positionVaultOutputAta: Address<TAccountPositionVaultOutputAta>;
  /**
   * Proposer's ATA for the output mint (tokens transferred TO here).
   * Owner-equality check (== position_settle.proposer) is in the handler.
   */
  proposerOutputAta: Address<TAccountProposerOutputAta>;
  /**
   * Tax reserve's ATA for the output mint. Optional — only required when
   * `Config.tax_reserve != default && Config.tax_bps > 0`. Owner-equality
   * check (== Config.tax_reserve) runs in the handler.
   */
  taxReserveOutputAta?: Address<TAccountTaxReserveOutputAta>;
  outputMint: Address<TAccountOutputMint>;
  tokenProgram: Address<TAccountTokenProgram>;
};

export async function getSettleProposerInstructionAsync<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountUserVault extends string,
  TAccountPositionSettle extends string,
  TAccountVault extends string,
  TAccountMeteoraPosition extends string,
  TAccountPositionVaultOutputAta extends string,
  TAccountProposerOutputAta extends string,
  TAccountTaxReserveOutputAta extends string,
  TAccountOutputMint extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: SettleProposerAsyncInput<
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountPositionSettle,
    TAccountVault,
    TAccountMeteoraPosition,
    TAccountPositionVaultOutputAta,
    TAccountProposerOutputAta,
    TAccountTaxReserveOutputAta,
    TAccountOutputMint,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  SettleProposerInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountPositionSettle,
    TAccountVault,
    TAccountMeteoraPosition,
    TAccountPositionVaultOutputAta,
    TAccountProposerOutputAta,
    TAccountTaxReserveOutputAta,
    TAccountOutputMint,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    userVault: { value: input.userVault ?? null, isWritable: false },
    positionSettle: { value: input.positionSettle ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: false },
    meteoraPosition: {
      value: input.meteoraPosition ?? null,
      isWritable: false,
    },
    positionVaultOutputAta: {
      value: input.positionVaultOutputAta ?? null,
      isWritable: true,
    },
    proposerOutputAta: {
      value: input.proposerOutputAta ?? null,
      isWritable: true,
    },
    taxReserveOutputAta: {
      value: input.taxReserveOutputAta ?? null,
      isWritable: true,
    },
    outputMint: { value: input.outputMint ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }
  if (!accounts.positionSettle.value) {
    accounts.positionSettle.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([112, 111, 115, 95, 115, 101, 116, 116, 108, 101])
        ),
        getAddressEncoder().encode(
          expectAddress(accounts.meteoraPosition.value)
        ),
      ],
    });
  }
  if (!accounts.vault.value) {
    accounts.vault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([118, 97, 117, 108, 116])),
        getAddressEncoder().encode(
          expectAddress(accounts.meteoraPosition.value)
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.userVault),
      getAccountMeta(accounts.positionSettle),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.meteoraPosition),
      getAccountMeta(accounts.positionVaultOutputAta),
      getAccountMeta(accounts.proposerOutputAta),
      getAccountMeta(accounts.taxReserveOutputAta),
      getAccountMeta(accounts.outputMint),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getSettleProposerInstructionDataEncoder().encode({}),
    programAddress,
  } as SettleProposerInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountPositionSettle,
    TAccountVault,
    TAccountMeteoraPosition,
    TAccountPositionVaultOutputAta,
    TAccountProposerOutputAta,
    TAccountTaxReserveOutputAta,
    TAccountOutputMint,
    TAccountTokenProgram
  >);
}

export type SettleProposerInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountUserVault extends string = string,
  TAccountPositionSettle extends string = string,
  TAccountVault extends string = string,
  TAccountMeteoraPosition extends string = string,
  TAccountPositionVaultOutputAta extends string = string,
  TAccountProposerOutputAta extends string = string,
  TAccountTaxReserveOutputAta extends string = string,
  TAccountOutputMint extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /** Bot or vault.owner — dual-caller, mirrors user_close. */
  caller: TransactionSigner<TAccountCaller>;
  config: Address<TAccountConfig>;
  /**
   * Treasury user_vault — vault.owner check is implicit via PDA seeding,
   * authorization checked in handler against caller.
   */
  userVault: Address<TAccountUserVault>;
  positionSettle: Address<TAccountPositionSettle>;
  /** Per-position vault PDA — signs the SPL Token transfer to proposer. */
  vault: Address<TAccountVault>;
  meteoraPosition: Address<TAccountMeteoraPosition>;
  /** Position vault's ATA for the output mint (tokens transferred FROM here). */
  positionVaultOutputAta: Address<TAccountPositionVaultOutputAta>;
  /**
   * Proposer's ATA for the output mint (tokens transferred TO here).
   * Owner-equality check (== position_settle.proposer) is in the handler.
   */
  proposerOutputAta: Address<TAccountProposerOutputAta>;
  /**
   * Tax reserve's ATA for the output mint. Optional — only required when
   * `Config.tax_reserve != default && Config.tax_bps > 0`. Owner-equality
   * check (== Config.tax_reserve) runs in the handler.
   */
  taxReserveOutputAta?: Address<TAccountTaxReserveOutputAta>;
  outputMint: Address<TAccountOutputMint>;
  tokenProgram: Address<TAccountTokenProgram>;
};

export function getSettleProposerInstruction<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountUserVault extends string,
  TAccountPositionSettle extends string,
  TAccountVault extends string,
  TAccountMeteoraPosition extends string,
  TAccountPositionVaultOutputAta extends string,
  TAccountProposerOutputAta extends string,
  TAccountTaxReserveOutputAta extends string,
  TAccountOutputMint extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: SettleProposerInput<
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountPositionSettle,
    TAccountVault,
    TAccountMeteoraPosition,
    TAccountPositionVaultOutputAta,
    TAccountProposerOutputAta,
    TAccountTaxReserveOutputAta,
    TAccountOutputMint,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): SettleProposerInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountConfig,
  TAccountUserVault,
  TAccountPositionSettle,
  TAccountVault,
  TAccountMeteoraPosition,
  TAccountPositionVaultOutputAta,
  TAccountProposerOutputAta,
  TAccountTaxReserveOutputAta,
  TAccountOutputMint,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    userVault: { value: input.userVault ?? null, isWritable: false },
    positionSettle: { value: input.positionSettle ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: false },
    meteoraPosition: {
      value: input.meteoraPosition ?? null,
      isWritable: false,
    },
    positionVaultOutputAta: {
      value: input.positionVaultOutputAta ?? null,
      isWritable: true,
    },
    proposerOutputAta: {
      value: input.proposerOutputAta ?? null,
      isWritable: true,
    },
    taxReserveOutputAta: {
      value: input.taxReserveOutputAta ?? null,
      isWritable: true,
    },
    outputMint: { value: input.outputMint ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.userVault),
      getAccountMeta(accounts.positionSettle),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.meteoraPosition),
      getAccountMeta(accounts.positionVaultOutputAta),
      getAccountMeta(accounts.proposerOutputAta),
      getAccountMeta(accounts.taxReserveOutputAta),
      getAccountMeta(accounts.outputMint),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getSettleProposerInstructionDataEncoder().encode({}),
    programAddress,
  } as SettleProposerInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountPositionSettle,
    TAccountVault,
    TAccountMeteoraPosition,
    TAccountPositionVaultOutputAta,
    TAccountProposerOutputAta,
    TAccountTaxReserveOutputAta,
    TAccountOutputMint,
    TAccountTokenProgram
  >);
}

export type ParsedSettleProposerInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Bot or vault.owner — dual-caller, mirrors user_close. */
    caller: TAccountMetas[0];
    config: TAccountMetas[1];
    /**
     * Treasury user_vault — vault.owner check is implicit via PDA seeding,
     * authorization checked in handler against caller.
     */
    userVault: TAccountMetas[2];
    positionSettle: TAccountMetas[3];
    /** Per-position vault PDA — signs the SPL Token transfer to proposer. */
    vault: TAccountMetas[4];
    meteoraPosition: TAccountMetas[5];
    /** Position vault's ATA for the output mint (tokens transferred FROM here). */
    positionVaultOutputAta: TAccountMetas[6];
    /**
     * Proposer's ATA for the output mint (tokens transferred TO here).
     * Owner-equality check (== position_settle.proposer) is in the handler.
     */
    proposerOutputAta: TAccountMetas[7];
    /**
     * Tax reserve's ATA for the output mint. Optional — only required when
     * `Config.tax_reserve != default && Config.tax_bps > 0`. Owner-equality
     * check (== Config.tax_reserve) runs in the handler.
     */
    taxReserveOutputAta?: TAccountMetas[8] | undefined;
    outputMint: TAccountMetas[9];
    tokenProgram: TAccountMetas[10];
  };
  data: SettleProposerInstructionData;
};

export function parseSettleProposerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSettleProposerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 11) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === BIN_FARM_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      config: getNextAccount(),
      userVault: getNextAccount(),
      positionSettle: getNextAccount(),
      vault: getNextAccount(),
      meteoraPosition: getNextAccount(),
      positionVaultOutputAta: getNextAccount(),
      proposerOutputAta: getNextAccount(),
      taxReserveOutputAta: getNextOptionalAccount(),
      outputMint: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getSettleProposerInstructionDataDecoder().decode(instruction.data),
  };
}
