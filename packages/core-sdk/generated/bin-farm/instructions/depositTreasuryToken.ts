/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { BIN_FARM_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const DEPOSIT_TREASURY_TOKEN_DISCRIMINATOR = new Uint8Array([
  63, 196, 128, 255, 53, 51, 103, 82,
]);

export function getDepositTreasuryTokenDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DEPOSIT_TREASURY_TOKEN_DISCRIMINATOR
  );
}

export type DepositTreasuryTokenInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountUserVault extends string | AccountMeta<string> = string,
  TAccountTokenMint extends string | AccountMeta<string> = string,
  TAccountOwnerTokenAccount extends string | AccountMeta<string> = string,
  TAccountVaultTokenAccount extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountUserVault extends string
        ? WritableAccount<TAccountUserVault>
        : TAccountUserVault,
      TAccountTokenMint extends string
        ? ReadonlyAccount<TAccountTokenMint>
        : TAccountTokenMint,
      TAccountOwnerTokenAccount extends string
        ? WritableAccount<TAccountOwnerTokenAccount>
        : TAccountOwnerTokenAccount,
      TAccountVaultTokenAccount extends string
        ? WritableAccount<TAccountVaultTokenAccount>
        : TAccountVaultTokenAccount,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type DepositTreasuryTokenInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type DepositTreasuryTokenInstructionDataArgs = {
  amount: number | bigint;
};

export function getDepositTreasuryTokenInstructionDataEncoder(): FixedSizeEncoder<DepositTreasuryTokenInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['amount', getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: DEPOSIT_TREASURY_TOKEN_DISCRIMINATOR,
    })
  );
}

export function getDepositTreasuryTokenInstructionDataDecoder(): FixedSizeDecoder<DepositTreasuryTokenInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['amount', getU64Decoder()],
  ]);
}

export function getDepositTreasuryTokenInstructionDataCodec(): FixedSizeCodec<
  DepositTreasuryTokenInstructionDataArgs,
  DepositTreasuryTokenInstructionData
> {
  return combineCodec(
    getDepositTreasuryTokenInstructionDataEncoder(),
    getDepositTreasuryTokenInstructionDataDecoder()
  );
}

export type DepositTreasuryTokenAsyncInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountUserVault extends string = string,
  TAccountTokenMint extends string = string,
  TAccountOwnerTokenAccount extends string = string,
  TAccountVaultTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /**
   * Must equal `user_vault.owner`. For the treasury vault that's the
   * Native Treasury PDA, signable only via SPL Governance executeTransaction.
   */
  caller: TransactionSigner<TAccountCaller>;
  config?: Address<TAccountConfig>;
  userVault: Address<TAccountUserVault>;
  tokenMint: Address<TAccountTokenMint>;
  /**
   * Source: caller-owned ATA (for treasury vault, this is NTP's direct ATA —
   * the one Realms displays).
   */
  ownerTokenAccount: Address<TAccountOwnerTokenAccount>;
  /** Destination: vault PDA's ATA. */
  vaultTokenAccount: Address<TAccountVaultTokenAccount>;
  tokenProgram: Address<TAccountTokenProgram>;
  amount: DepositTreasuryTokenInstructionDataArgs['amount'];
};

export async function getDepositTreasuryTokenInstructionAsync<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountUserVault extends string,
  TAccountTokenMint extends string,
  TAccountOwnerTokenAccount extends string,
  TAccountVaultTokenAccount extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: DepositTreasuryTokenAsyncInput<
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountTokenMint,
    TAccountOwnerTokenAccount,
    TAccountVaultTokenAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  DepositTreasuryTokenInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountTokenMint,
    TAccountOwnerTokenAccount,
    TAccountVaultTokenAccount,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    userVault: { value: input.userVault ?? null, isWritable: true },
    tokenMint: { value: input.tokenMint ?? null, isWritable: false },
    ownerTokenAccount: {
      value: input.ownerTokenAccount ?? null,
      isWritable: true,
    },
    vaultTokenAccount: {
      value: input.vaultTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.userVault),
      getAccountMeta(accounts.tokenMint),
      getAccountMeta(accounts.ownerTokenAccount),
      getAccountMeta(accounts.vaultTokenAccount),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getDepositTreasuryTokenInstructionDataEncoder().encode(
      args as DepositTreasuryTokenInstructionDataArgs
    ),
    programAddress,
  } as DepositTreasuryTokenInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountTokenMint,
    TAccountOwnerTokenAccount,
    TAccountVaultTokenAccount,
    TAccountTokenProgram
  >);
}

export type DepositTreasuryTokenInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountUserVault extends string = string,
  TAccountTokenMint extends string = string,
  TAccountOwnerTokenAccount extends string = string,
  TAccountVaultTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /**
   * Must equal `user_vault.owner`. For the treasury vault that's the
   * Native Treasury PDA, signable only via SPL Governance executeTransaction.
   */
  caller: TransactionSigner<TAccountCaller>;
  config: Address<TAccountConfig>;
  userVault: Address<TAccountUserVault>;
  tokenMint: Address<TAccountTokenMint>;
  /**
   * Source: caller-owned ATA (for treasury vault, this is NTP's direct ATA —
   * the one Realms displays).
   */
  ownerTokenAccount: Address<TAccountOwnerTokenAccount>;
  /** Destination: vault PDA's ATA. */
  vaultTokenAccount: Address<TAccountVaultTokenAccount>;
  tokenProgram: Address<TAccountTokenProgram>;
  amount: DepositTreasuryTokenInstructionDataArgs['amount'];
};

export function getDepositTreasuryTokenInstruction<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountUserVault extends string,
  TAccountTokenMint extends string,
  TAccountOwnerTokenAccount extends string,
  TAccountVaultTokenAccount extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: DepositTreasuryTokenInput<
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountTokenMint,
    TAccountOwnerTokenAccount,
    TAccountVaultTokenAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): DepositTreasuryTokenInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountConfig,
  TAccountUserVault,
  TAccountTokenMint,
  TAccountOwnerTokenAccount,
  TAccountVaultTokenAccount,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    userVault: { value: input.userVault ?? null, isWritable: true },
    tokenMint: { value: input.tokenMint ?? null, isWritable: false },
    ownerTokenAccount: {
      value: input.ownerTokenAccount ?? null,
      isWritable: true,
    },
    vaultTokenAccount: {
      value: input.vaultTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.userVault),
      getAccountMeta(accounts.tokenMint),
      getAccountMeta(accounts.ownerTokenAccount),
      getAccountMeta(accounts.vaultTokenAccount),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getDepositTreasuryTokenInstructionDataEncoder().encode(
      args as DepositTreasuryTokenInstructionDataArgs
    ),
    programAddress,
  } as DepositTreasuryTokenInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountTokenMint,
    TAccountOwnerTokenAccount,
    TAccountVaultTokenAccount,
    TAccountTokenProgram
  >);
}

export type ParsedDepositTreasuryTokenInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Must equal `user_vault.owner`. For the treasury vault that's the
     * Native Treasury PDA, signable only via SPL Governance executeTransaction.
     */
    caller: TAccountMetas[0];
    config: TAccountMetas[1];
    userVault: TAccountMetas[2];
    tokenMint: TAccountMetas[3];
    /**
     * Source: caller-owned ATA (for treasury vault, this is NTP's direct ATA —
     * the one Realms displays).
     */
    ownerTokenAccount: TAccountMetas[4];
    /** Destination: vault PDA's ATA. */
    vaultTokenAccount: TAccountMetas[5];
    tokenProgram: TAccountMetas[6];
  };
  data: DepositTreasuryTokenInstructionData;
};

export function parseDepositTreasuryTokenInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedDepositTreasuryTokenInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      config: getNextAccount(),
      userVault: getNextAccount(),
      tokenMint: getNextAccount(),
      ownerTokenAccount: getNextAccount(),
      vaultTokenAccount: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getDepositTreasuryTokenInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
