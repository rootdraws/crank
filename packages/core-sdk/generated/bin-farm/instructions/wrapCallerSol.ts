/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { BIN_FARM_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const WRAP_CALLER_SOL_DISCRIMINATOR = new Uint8Array([
  203, 147, 177, 25, 195, 217, 13, 84,
]);

export function getWrapCallerSolDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    WRAP_CALLER_SOL_DISCRIMINATOR
  );
}

export type WrapCallerSolInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountCallerWsolAta extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountCallerWsolAta extends string
        ? WritableAccount<TAccountCallerWsolAta>
        : TAccountCallerWsolAta,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type WrapCallerSolInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type WrapCallerSolInstructionDataArgs = { amount: number | bigint };

export function getWrapCallerSolInstructionDataEncoder(): FixedSizeEncoder<WrapCallerSolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['amount', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: WRAP_CALLER_SOL_DISCRIMINATOR })
  );
}

export function getWrapCallerSolInstructionDataDecoder(): FixedSizeDecoder<WrapCallerSolInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['amount', getU64Decoder()],
  ]);
}

export function getWrapCallerSolInstructionDataCodec(): FixedSizeCodec<
  WrapCallerSolInstructionDataArgs,
  WrapCallerSolInstructionData
> {
  return combineCodec(
    getWrapCallerSolInstructionDataEncoder(),
    getWrapCallerSolInstructionDataDecoder()
  );
}

export type WrapCallerSolInput<
  TAccountCaller extends string = string,
  TAccountCallerWsolAta extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  caller: TransactionSigner<TAccountCaller>;
  callerWsolAta: Address<TAccountCallerWsolAta>;
  tokenProgram: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: WrapCallerSolInstructionDataArgs['amount'];
};

export function getWrapCallerSolInstruction<
  TAccountCaller extends string,
  TAccountCallerWsolAta extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: WrapCallerSolInput<
    TAccountCaller,
    TAccountCallerWsolAta,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): WrapCallerSolInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountCallerWsolAta,
  TAccountTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    callerWsolAta: { value: input.callerWsolAta ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.callerWsolAta),
      getAccountMeta(accounts.tokenProgram),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getWrapCallerSolInstructionDataEncoder().encode(
      args as WrapCallerSolInstructionDataArgs
    ),
    programAddress,
  } as WrapCallerSolInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountCallerWsolAta,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type ParsedWrapCallerSolInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    caller: TAccountMetas[0];
    callerWsolAta: TAccountMetas[1];
    tokenProgram: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: WrapCallerSolInstructionData;
};

export function parseWrapCallerSolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedWrapCallerSolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      callerWsolAta: getNextAccount(),
      tokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getWrapCallerSolInstructionDataDecoder().decode(instruction.data),
  };
}
