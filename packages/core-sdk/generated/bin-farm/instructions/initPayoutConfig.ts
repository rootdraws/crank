/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { BIN_FARM_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const INIT_PAYOUT_CONFIG_DISCRIMINATOR = new Uint8Array([
  73, 17, 51, 86, 143, 36, 82, 34,
]);

export function getInitPayoutConfigDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_PAYOUT_CONFIG_DISCRIMINATOR
  );
}

export type InitPayoutConfigInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type InitPayoutConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
  payoutBps: number;
  matchRatioBps: number;
  payoutAdmin: Address;
};

export type InitPayoutConfigInstructionDataArgs = {
  payoutBps: number;
  matchRatioBps: number;
  payoutAdmin: Address;
};

export function getInitPayoutConfigInstructionDataEncoder(): FixedSizeEncoder<InitPayoutConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['payoutBps', getU16Encoder()],
      ['matchRatioBps', getU16Encoder()],
      ['payoutAdmin', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_PAYOUT_CONFIG_DISCRIMINATOR })
  );
}

export function getInitPayoutConfigInstructionDataDecoder(): FixedSizeDecoder<InitPayoutConfigInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['payoutBps', getU16Decoder()],
    ['matchRatioBps', getU16Decoder()],
    ['payoutAdmin', getAddressDecoder()],
  ]);
}

export function getInitPayoutConfigInstructionDataCodec(): FixedSizeCodec<
  InitPayoutConfigInstructionDataArgs,
  InitPayoutConfigInstructionData
> {
  return combineCodec(
    getInitPayoutConfigInstructionDataEncoder(),
    getInitPayoutConfigInstructionDataDecoder()
  );
}

export type InitPayoutConfigAsyncInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config?: Address<TAccountConfig>;
  payoutBps: InitPayoutConfigInstructionDataArgs['payoutBps'];
  matchRatioBps: InitPayoutConfigInstructionDataArgs['matchRatioBps'];
  payoutAdmin: InitPayoutConfigInstructionDataArgs['payoutAdmin'];
};

export async function getInitPayoutConfigInstructionAsync<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: InitPayoutConfigAsyncInput<TAccountAuthority, TAccountConfig>,
  config?: { programAddress?: TProgramAddress }
): Promise<
  InitPayoutConfigInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.config),
    ],
    data: getInitPayoutConfigInstructionDataEncoder().encode(
      args as InitPayoutConfigInstructionDataArgs
    ),
    programAddress,
  } as InitPayoutConfigInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig
  >);
}

export type InitPayoutConfigInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config: Address<TAccountConfig>;
  payoutBps: InitPayoutConfigInstructionDataArgs['payoutBps'];
  matchRatioBps: InitPayoutConfigInstructionDataArgs['matchRatioBps'];
  payoutAdmin: InitPayoutConfigInstructionDataArgs['payoutAdmin'];
};

export function getInitPayoutConfigInstruction<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: InitPayoutConfigInput<TAccountAuthority, TAccountConfig>,
  config?: { programAddress?: TProgramAddress }
): InitPayoutConfigInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.config),
    ],
    data: getInitPayoutConfigInstructionDataEncoder().encode(
      args as InitPayoutConfigInstructionDataArgs
    ),
    programAddress,
  } as InitPayoutConfigInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig
  >);
}

export type ParsedInitPayoutConfigInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    config: TAccountMetas[1];
  };
  data: InitPayoutConfigInstructionData;
};

export function parseInitPayoutConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedInitPayoutConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), config: getNextAccount() },
    data: getInitPayoutConfigInstructionDataDecoder().decode(instruction.data),
  };
}
