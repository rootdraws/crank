/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { BIN_FARM_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const UNWRAP_WSOL_IN_VAULT_DISCRIMINATOR = new Uint8Array([
  190, 241, 246, 59, 88, 255, 211, 53,
]);

export function getUnwrapWsolInVaultDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UNWRAP_WSOL_IN_VAULT_DISCRIMINATOR
  );
}

export type UnwrapWsolInVaultInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountUserVault extends string | AccountMeta<string> = string,
  TAccountVaultWsolAta extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountUserVault extends string
        ? WritableAccount<TAccountUserVault>
        : TAccountUserVault,
      TAccountVaultWsolAta extends string
        ? WritableAccount<TAccountVaultWsolAta>
        : TAccountVaultWsolAta,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type UnwrapWsolInVaultInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type UnwrapWsolInVaultInstructionDataArgs = {};

export function getUnwrapWsolInVaultInstructionDataEncoder(): FixedSizeEncoder<UnwrapWsolInVaultInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: UNWRAP_WSOL_IN_VAULT_DISCRIMINATOR })
  );
}

export function getUnwrapWsolInVaultInstructionDataDecoder(): FixedSizeDecoder<UnwrapWsolInVaultInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getUnwrapWsolInVaultInstructionDataCodec(): FixedSizeCodec<
  UnwrapWsolInVaultInstructionDataArgs,
  UnwrapWsolInVaultInstructionData
> {
  return combineCodec(
    getUnwrapWsolInVaultInstructionDataEncoder(),
    getUnwrapWsolInVaultInstructionDataDecoder()
  );
}

export type UnwrapWsolInVaultAsyncInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountUserVault extends string = string,
  TAccountVaultWsolAta extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  caller: TransactionSigner<TAccountCaller>;
  config?: Address<TAccountConfig>;
  userVault: Address<TAccountUserVault>;
  vaultWsolAta: Address<TAccountVaultWsolAta>;
  tokenProgram: Address<TAccountTokenProgram>;
};

export async function getUnwrapWsolInVaultInstructionAsync<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountUserVault extends string,
  TAccountVaultWsolAta extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: UnwrapWsolInVaultAsyncInput<
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountVaultWsolAta,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  UnwrapWsolInVaultInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountVaultWsolAta,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    userVault: { value: input.userVault ?? null, isWritable: true },
    vaultWsolAta: { value: input.vaultWsolAta ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.userVault),
      getAccountMeta(accounts.vaultWsolAta),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getUnwrapWsolInVaultInstructionDataEncoder().encode({}),
    programAddress,
  } as UnwrapWsolInVaultInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountVaultWsolAta,
    TAccountTokenProgram
  >);
}

export type UnwrapWsolInVaultInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountUserVault extends string = string,
  TAccountVaultWsolAta extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  caller: TransactionSigner<TAccountCaller>;
  config: Address<TAccountConfig>;
  userVault: Address<TAccountUserVault>;
  vaultWsolAta: Address<TAccountVaultWsolAta>;
  tokenProgram: Address<TAccountTokenProgram>;
};

export function getUnwrapWsolInVaultInstruction<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountUserVault extends string,
  TAccountVaultWsolAta extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: UnwrapWsolInVaultInput<
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountVaultWsolAta,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): UnwrapWsolInVaultInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountConfig,
  TAccountUserVault,
  TAccountVaultWsolAta,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    userVault: { value: input.userVault ?? null, isWritable: true },
    vaultWsolAta: { value: input.vaultWsolAta ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.userVault),
      getAccountMeta(accounts.vaultWsolAta),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getUnwrapWsolInVaultInstructionDataEncoder().encode({}),
    programAddress,
  } as UnwrapWsolInVaultInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountVaultWsolAta,
    TAccountTokenProgram
  >);
}

export type ParsedUnwrapWsolInVaultInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    caller: TAccountMetas[0];
    config: TAccountMetas[1];
    userVault: TAccountMetas[2];
    vaultWsolAta: TAccountMetas[3];
    tokenProgram: TAccountMetas[4];
  };
  data: UnwrapWsolInVaultInstructionData;
};

export function parseUnwrapWsolInVaultInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUnwrapWsolInVaultInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      config: getNextAccount(),
      userVault: getNextAccount(),
      vaultWsolAta: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getUnwrapWsolInVaultInstructionDataDecoder().decode(instruction.data),
  };
}
