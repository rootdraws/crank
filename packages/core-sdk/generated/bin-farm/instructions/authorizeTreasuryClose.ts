/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { BIN_FARM_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const AUTHORIZE_TREASURY_CLOSE_DISCRIMINATOR = new Uint8Array([
  89, 219, 178, 7, 42, 244, 65, 113,
]);

export function getAuthorizeTreasuryCloseDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    AUTHORIZE_TREASURY_CLOSE_DISCRIMINATOR
  );
}

export type AuthorizeTreasuryCloseInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountUserVault extends string | AccountMeta<string> = string,
  TAccountLbPair extends string | AccountMeta<string> = string,
  TAccountTradeAuth extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountUserVault extends string
        ? ReadonlyAccount<TAccountUserVault>
        : TAccountUserVault,
      TAccountLbPair extends string
        ? ReadonlyAccount<TAccountLbPair>
        : TAccountLbPair,
      TAccountTradeAuth extends string
        ? WritableAccount<TAccountTradeAuth>
        : TAccountTradeAuth,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AuthorizeTreasuryCloseInstructionData = {
  discriminator: ReadonlyUint8Array;
  proposer: Address;
};

export type AuthorizeTreasuryCloseInstructionDataArgs = { proposer: Address };

export function getAuthorizeTreasuryCloseInstructionDataEncoder(): FixedSizeEncoder<AuthorizeTreasuryCloseInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['proposer', getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: AUTHORIZE_TREASURY_CLOSE_DISCRIMINATOR,
    })
  );
}

export function getAuthorizeTreasuryCloseInstructionDataDecoder(): FixedSizeDecoder<AuthorizeTreasuryCloseInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['proposer', getAddressDecoder()],
  ]);
}

export function getAuthorizeTreasuryCloseInstructionDataCodec(): FixedSizeCodec<
  AuthorizeTreasuryCloseInstructionDataArgs,
  AuthorizeTreasuryCloseInstructionData
> {
  return combineCodec(
    getAuthorizeTreasuryCloseInstructionDataEncoder(),
    getAuthorizeTreasuryCloseInstructionDataDecoder()
  );
}

export type AuthorizeTreasuryCloseAsyncInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountUserVault extends string = string,
  TAccountLbPair extends string = string,
  TAccountTradeAuth extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  caller: TransactionSigner<TAccountCaller>;
  config?: Address<TAccountConfig>;
  userVault: Address<TAccountUserVault>;
  lbPair: Address<TAccountLbPair>;
  tradeAuth?: Address<TAccountTradeAuth>;
  systemProgram?: Address<TAccountSystemProgram>;
  proposer: AuthorizeTreasuryCloseInstructionDataArgs['proposer'];
};

export async function getAuthorizeTreasuryCloseInstructionAsync<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountUserVault extends string,
  TAccountLbPair extends string,
  TAccountTradeAuth extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: AuthorizeTreasuryCloseAsyncInput<
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountLbPair,
    TAccountTradeAuth,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  AuthorizeTreasuryCloseInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountLbPair,
    TAccountTradeAuth,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    userVault: { value: input.userVault ?? null, isWritable: false },
    lbPair: { value: input.lbPair ?? null, isWritable: false },
    tradeAuth: { value: input.tradeAuth ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }
  if (!accounts.tradeAuth.value) {
    accounts.tradeAuth.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([116, 114, 97, 100, 101, 95, 97, 117, 116, 104])
        ),
        getAddressEncoder().encode(expectAddress(accounts.userVault.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.userVault),
      getAccountMeta(accounts.lbPair),
      getAccountMeta(accounts.tradeAuth),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getAuthorizeTreasuryCloseInstructionDataEncoder().encode(
      args as AuthorizeTreasuryCloseInstructionDataArgs
    ),
    programAddress,
  } as AuthorizeTreasuryCloseInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountLbPair,
    TAccountTradeAuth,
    TAccountSystemProgram
  >);
}

export type AuthorizeTreasuryCloseInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountUserVault extends string = string,
  TAccountLbPair extends string = string,
  TAccountTradeAuth extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  caller: TransactionSigner<TAccountCaller>;
  config: Address<TAccountConfig>;
  userVault: Address<TAccountUserVault>;
  lbPair: Address<TAccountLbPair>;
  tradeAuth: Address<TAccountTradeAuth>;
  systemProgram?: Address<TAccountSystemProgram>;
  proposer: AuthorizeTreasuryCloseInstructionDataArgs['proposer'];
};

export function getAuthorizeTreasuryCloseInstruction<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountUserVault extends string,
  TAccountLbPair extends string,
  TAccountTradeAuth extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: AuthorizeTreasuryCloseInput<
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountLbPair,
    TAccountTradeAuth,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): AuthorizeTreasuryCloseInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountConfig,
  TAccountUserVault,
  TAccountLbPair,
  TAccountTradeAuth,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    userVault: { value: input.userVault ?? null, isWritable: false },
    lbPair: { value: input.lbPair ?? null, isWritable: false },
    tradeAuth: { value: input.tradeAuth ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.userVault),
      getAccountMeta(accounts.lbPair),
      getAccountMeta(accounts.tradeAuth),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getAuthorizeTreasuryCloseInstructionDataEncoder().encode(
      args as AuthorizeTreasuryCloseInstructionDataArgs
    ),
    programAddress,
  } as AuthorizeTreasuryCloseInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountLbPair,
    TAccountTradeAuth,
    TAccountSystemProgram
  >);
}

export type ParsedAuthorizeTreasuryCloseInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    caller: TAccountMetas[0];
    config: TAccountMetas[1];
    userVault: TAccountMetas[2];
    lbPair: TAccountMetas[3];
    tradeAuth: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: AuthorizeTreasuryCloseInstructionData;
};

export function parseAuthorizeTreasuryCloseInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedAuthorizeTreasuryCloseInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      config: getNextAccount(),
      userVault: getNextAccount(),
      lbPair: getNextAccount(),
      tradeAuth: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAuthorizeTreasuryCloseInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
