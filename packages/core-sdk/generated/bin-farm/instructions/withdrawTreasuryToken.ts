/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { BIN_FARM_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const WITHDRAW_TREASURY_TOKEN_DISCRIMINATOR = new Uint8Array([
  63, 112, 138, 126, 181, 23, 101, 154,
]);

export function getWithdrawTreasuryTokenDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    WITHDRAW_TREASURY_TOKEN_DISCRIMINATOR
  );
}

export type WithdrawTreasuryTokenInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountUserVault extends string | AccountMeta<string> = string,
  TAccountTokenMint extends string | AccountMeta<string> = string,
  TAccountVaultTokenAccount extends string | AccountMeta<string> = string,
  TAccountOwnerTokenAccount extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountUserVault extends string
        ? WritableAccount<TAccountUserVault>
        : TAccountUserVault,
      TAccountTokenMint extends string
        ? ReadonlyAccount<TAccountTokenMint>
        : TAccountTokenMint,
      TAccountVaultTokenAccount extends string
        ? WritableAccount<TAccountVaultTokenAccount>
        : TAccountVaultTokenAccount,
      TAccountOwnerTokenAccount extends string
        ? WritableAccount<TAccountOwnerTokenAccount>
        : TAccountOwnerTokenAccount,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type WithdrawTreasuryTokenInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type WithdrawTreasuryTokenInstructionDataArgs = {
  amount: number | bigint;
};

export function getWithdrawTreasuryTokenInstructionDataEncoder(): FixedSizeEncoder<WithdrawTreasuryTokenInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['amount', getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: WITHDRAW_TREASURY_TOKEN_DISCRIMINATOR,
    })
  );
}

export function getWithdrawTreasuryTokenInstructionDataDecoder(): FixedSizeDecoder<WithdrawTreasuryTokenInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['amount', getU64Decoder()],
  ]);
}

export function getWithdrawTreasuryTokenInstructionDataCodec(): FixedSizeCodec<
  WithdrawTreasuryTokenInstructionDataArgs,
  WithdrawTreasuryTokenInstructionData
> {
  return combineCodec(
    getWithdrawTreasuryTokenInstructionDataEncoder(),
    getWithdrawTreasuryTokenInstructionDataDecoder()
  );
}

export type WithdrawTreasuryTokenAsyncInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountUserVault extends string = string,
  TAccountTokenMint extends string = string,
  TAccountVaultTokenAccount extends string = string,
  TAccountOwnerTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /**
   * Must equal `user_vault.owner`. For the treasury vault that's the
   * Native Treasury PDA, signable only via SPL Governance executeTransaction.
   */
  caller: TransactionSigner<TAccountCaller>;
  config?: Address<TAccountConfig>;
  userVault: Address<TAccountUserVault>;
  tokenMint: Address<TAccountTokenMint>;
  /** Source: vault PDA's ATA. Authority is the vault PDA (signed by seeds). */
  vaultTokenAccount: Address<TAccountVaultTokenAccount>;
  /**
   * Destination: caller-owned ATA (for treasury vault, this is NTP's direct
   * ATA — restores Realms-visible reserves).
   */
  ownerTokenAccount: Address<TAccountOwnerTokenAccount>;
  tokenProgram: Address<TAccountTokenProgram>;
  amount: WithdrawTreasuryTokenInstructionDataArgs['amount'];
};

export async function getWithdrawTreasuryTokenInstructionAsync<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountUserVault extends string,
  TAccountTokenMint extends string,
  TAccountVaultTokenAccount extends string,
  TAccountOwnerTokenAccount extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: WithdrawTreasuryTokenAsyncInput<
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountTokenMint,
    TAccountVaultTokenAccount,
    TAccountOwnerTokenAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  WithdrawTreasuryTokenInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountTokenMint,
    TAccountVaultTokenAccount,
    TAccountOwnerTokenAccount,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    userVault: { value: input.userVault ?? null, isWritable: true },
    tokenMint: { value: input.tokenMint ?? null, isWritable: false },
    vaultTokenAccount: {
      value: input.vaultTokenAccount ?? null,
      isWritable: true,
    },
    ownerTokenAccount: {
      value: input.ownerTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.userVault),
      getAccountMeta(accounts.tokenMint),
      getAccountMeta(accounts.vaultTokenAccount),
      getAccountMeta(accounts.ownerTokenAccount),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getWithdrawTreasuryTokenInstructionDataEncoder().encode(
      args as WithdrawTreasuryTokenInstructionDataArgs
    ),
    programAddress,
  } as WithdrawTreasuryTokenInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountTokenMint,
    TAccountVaultTokenAccount,
    TAccountOwnerTokenAccount,
    TAccountTokenProgram
  >);
}

export type WithdrawTreasuryTokenInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountUserVault extends string = string,
  TAccountTokenMint extends string = string,
  TAccountVaultTokenAccount extends string = string,
  TAccountOwnerTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /**
   * Must equal `user_vault.owner`. For the treasury vault that's the
   * Native Treasury PDA, signable only via SPL Governance executeTransaction.
   */
  caller: TransactionSigner<TAccountCaller>;
  config: Address<TAccountConfig>;
  userVault: Address<TAccountUserVault>;
  tokenMint: Address<TAccountTokenMint>;
  /** Source: vault PDA's ATA. Authority is the vault PDA (signed by seeds). */
  vaultTokenAccount: Address<TAccountVaultTokenAccount>;
  /**
   * Destination: caller-owned ATA (for treasury vault, this is NTP's direct
   * ATA — restores Realms-visible reserves).
   */
  ownerTokenAccount: Address<TAccountOwnerTokenAccount>;
  tokenProgram: Address<TAccountTokenProgram>;
  amount: WithdrawTreasuryTokenInstructionDataArgs['amount'];
};

export function getWithdrawTreasuryTokenInstruction<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountUserVault extends string,
  TAccountTokenMint extends string,
  TAccountVaultTokenAccount extends string,
  TAccountOwnerTokenAccount extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: WithdrawTreasuryTokenInput<
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountTokenMint,
    TAccountVaultTokenAccount,
    TAccountOwnerTokenAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): WithdrawTreasuryTokenInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountConfig,
  TAccountUserVault,
  TAccountTokenMint,
  TAccountVaultTokenAccount,
  TAccountOwnerTokenAccount,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    userVault: { value: input.userVault ?? null, isWritable: true },
    tokenMint: { value: input.tokenMint ?? null, isWritable: false },
    vaultTokenAccount: {
      value: input.vaultTokenAccount ?? null,
      isWritable: true,
    },
    ownerTokenAccount: {
      value: input.ownerTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.userVault),
      getAccountMeta(accounts.tokenMint),
      getAccountMeta(accounts.vaultTokenAccount),
      getAccountMeta(accounts.ownerTokenAccount),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getWithdrawTreasuryTokenInstructionDataEncoder().encode(
      args as WithdrawTreasuryTokenInstructionDataArgs
    ),
    programAddress,
  } as WithdrawTreasuryTokenInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountTokenMint,
    TAccountVaultTokenAccount,
    TAccountOwnerTokenAccount,
    TAccountTokenProgram
  >);
}

export type ParsedWithdrawTreasuryTokenInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Must equal `user_vault.owner`. For the treasury vault that's the
     * Native Treasury PDA, signable only via SPL Governance executeTransaction.
     */
    caller: TAccountMetas[0];
    config: TAccountMetas[1];
    userVault: TAccountMetas[2];
    tokenMint: TAccountMetas[3];
    /** Source: vault PDA's ATA. Authority is the vault PDA (signed by seeds). */
    vaultTokenAccount: TAccountMetas[4];
    /**
     * Destination: caller-owned ATA (for treasury vault, this is NTP's direct
     * ATA — restores Realms-visible reserves).
     */
    ownerTokenAccount: TAccountMetas[5];
    tokenProgram: TAccountMetas[6];
  };
  data: WithdrawTreasuryTokenInstructionData;
};

export function parseWithdrawTreasuryTokenInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedWithdrawTreasuryTokenInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      config: getNextAccount(),
      userVault: getNextAccount(),
      tokenMint: getNextAccount(),
      vaultTokenAccount: getNextAccount(),
      ownerTokenAccount: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getWithdrawTreasuryTokenInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
