/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { BIN_FARM_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const WRAP_BURN_SOL_DISCRIMINATOR = new Uint8Array([
  253, 0, 187, 181, 140, 15, 4, 26,
]);

export function getWrapBurnSolDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    WRAP_BURN_SOL_DISCRIMINATOR
  );
}

export type WrapBurnSolInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountRoverAuthority extends string | AccountMeta<string> = string,
  TAccountBurnSolVault extends string | AccountMeta<string> = string,
  TAccountRoverWsolAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? ReadonlySignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountRoverAuthority extends string
        ? ReadonlyAccount<TAccountRoverAuthority>
        : TAccountRoverAuthority,
      TAccountBurnSolVault extends string
        ? WritableAccount<TAccountBurnSolVault>
        : TAccountBurnSolVault,
      TAccountRoverWsolAccount extends string
        ? WritableAccount<TAccountRoverWsolAccount>
        : TAccountRoverWsolAccount,
      ...TRemainingAccounts,
    ]
  >;

export type WrapBurnSolInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type WrapBurnSolInstructionDataArgs = { amount: number | bigint };

export function getWrapBurnSolInstructionDataEncoder(): FixedSizeEncoder<WrapBurnSolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['amount', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: WRAP_BURN_SOL_DISCRIMINATOR })
  );
}

export function getWrapBurnSolInstructionDataDecoder(): FixedSizeDecoder<WrapBurnSolInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['amount', getU64Decoder()],
  ]);
}

export function getWrapBurnSolInstructionDataCodec(): FixedSizeCodec<
  WrapBurnSolInstructionDataArgs,
  WrapBurnSolInstructionData
> {
  return combineCodec(
    getWrapBurnSolInstructionDataEncoder(),
    getWrapBurnSolInstructionDataDecoder()
  );
}

export type WrapBurnSolAsyncInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountRoverAuthority extends string = string,
  TAccountBurnSolVault extends string = string,
  TAccountRoverWsolAccount extends string = string,
> = {
  /** Bot-gated. */
  caller: TransactionSigner<TAccountCaller>;
  config?: Address<TAccountConfig>;
  roverAuthority?: Address<TAccountRoverAuthority>;
  burnSolVault?: Address<TAccountBurnSolVault>;
  /**
   * Rover authority's WSOL ATA. Receives lamports here; bot calls SPL
   * `sync_native` in the next ix to update the WSOL token balance.
   */
  roverWsolAccount: Address<TAccountRoverWsolAccount>;
  amount: WrapBurnSolInstructionDataArgs['amount'];
};

export async function getWrapBurnSolInstructionAsync<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountRoverAuthority extends string,
  TAccountBurnSolVault extends string,
  TAccountRoverWsolAccount extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: WrapBurnSolAsyncInput<
    TAccountCaller,
    TAccountConfig,
    TAccountRoverAuthority,
    TAccountBurnSolVault,
    TAccountRoverWsolAccount
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  WrapBurnSolInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountRoverAuthority,
    TAccountBurnSolVault,
    TAccountRoverWsolAccount
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    roverAuthority: { value: input.roverAuthority ?? null, isWritable: false },
    burnSolVault: { value: input.burnSolVault ?? null, isWritable: true },
    roverWsolAccount: {
      value: input.roverWsolAccount ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }
  if (!accounts.roverAuthority.value) {
    accounts.roverAuthority.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            114, 111, 118, 101, 114, 95, 97, 117, 116, 104, 111, 114, 105, 116,
            121,
          ])
        ),
      ],
    });
  }
  if (!accounts.burnSolVault.value) {
    accounts.burnSolVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            98, 117, 114, 110, 95, 115, 111, 108, 95, 118, 97, 117, 108, 116,
          ])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.roverAuthority),
      getAccountMeta(accounts.burnSolVault),
      getAccountMeta(accounts.roverWsolAccount),
    ],
    data: getWrapBurnSolInstructionDataEncoder().encode(
      args as WrapBurnSolInstructionDataArgs
    ),
    programAddress,
  } as WrapBurnSolInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountRoverAuthority,
    TAccountBurnSolVault,
    TAccountRoverWsolAccount
  >);
}

export type WrapBurnSolInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountRoverAuthority extends string = string,
  TAccountBurnSolVault extends string = string,
  TAccountRoverWsolAccount extends string = string,
> = {
  /** Bot-gated. */
  caller: TransactionSigner<TAccountCaller>;
  config: Address<TAccountConfig>;
  roverAuthority: Address<TAccountRoverAuthority>;
  burnSolVault: Address<TAccountBurnSolVault>;
  /**
   * Rover authority's WSOL ATA. Receives lamports here; bot calls SPL
   * `sync_native` in the next ix to update the WSOL token balance.
   */
  roverWsolAccount: Address<TAccountRoverWsolAccount>;
  amount: WrapBurnSolInstructionDataArgs['amount'];
};

export function getWrapBurnSolInstruction<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountRoverAuthority extends string,
  TAccountBurnSolVault extends string,
  TAccountRoverWsolAccount extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: WrapBurnSolInput<
    TAccountCaller,
    TAccountConfig,
    TAccountRoverAuthority,
    TAccountBurnSolVault,
    TAccountRoverWsolAccount
  >,
  config?: { programAddress?: TProgramAddress }
): WrapBurnSolInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountConfig,
  TAccountRoverAuthority,
  TAccountBurnSolVault,
  TAccountRoverWsolAccount
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    roverAuthority: { value: input.roverAuthority ?? null, isWritable: false },
    burnSolVault: { value: input.burnSolVault ?? null, isWritable: true },
    roverWsolAccount: {
      value: input.roverWsolAccount ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.roverAuthority),
      getAccountMeta(accounts.burnSolVault),
      getAccountMeta(accounts.roverWsolAccount),
    ],
    data: getWrapBurnSolInstructionDataEncoder().encode(
      args as WrapBurnSolInstructionDataArgs
    ),
    programAddress,
  } as WrapBurnSolInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountRoverAuthority,
    TAccountBurnSolVault,
    TAccountRoverWsolAccount
  >);
}

export type ParsedWrapBurnSolInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Bot-gated. */
    caller: TAccountMetas[0];
    config: TAccountMetas[1];
    roverAuthority: TAccountMetas[2];
    burnSolVault: TAccountMetas[3];
    /**
     * Rover authority's WSOL ATA. Receives lamports here; bot calls SPL
     * `sync_native` in the next ix to update the WSOL token balance.
     */
    roverWsolAccount: TAccountMetas[4];
  };
  data: WrapBurnSolInstructionData;
};

export function parseWrapBurnSolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedWrapBurnSolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      config: getNextAccount(),
      roverAuthority: getNextAccount(),
      burnSolVault: getNextAccount(),
      roverWsolAccount: getNextAccount(),
    },
    data: getWrapBurnSolInstructionDataDecoder().decode(instruction.data),
  };
}
