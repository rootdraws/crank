/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { BIN_FARM_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const CLOSE_SETTLE_DISCRIMINATOR = new Uint8Array([
  103, 225, 116, 192, 252, 122, 244, 82,
]);

export function getCloseSettleDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_SETTLE_DISCRIMINATOR
  );
}

export type CloseSettleInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountUserVault extends string | AccountMeta<string> = string,
  TAccountPositionSettle extends string | AccountMeta<string> = string,
  TAccountMeteoraPosition extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountUserVault extends string
        ? WritableAccount<TAccountUserVault>
        : TAccountUserVault,
      TAccountPositionSettle extends string
        ? WritableAccount<TAccountPositionSettle>
        : TAccountPositionSettle,
      TAccountMeteoraPosition extends string
        ? ReadonlyAccount<TAccountMeteoraPosition>
        : TAccountMeteoraPosition,
      ...TRemainingAccounts,
    ]
  >;

export type CloseSettleInstructionData = { discriminator: ReadonlyUint8Array };

export type CloseSettleInstructionDataArgs = {};

export function getCloseSettleInstructionDataEncoder(): FixedSizeEncoder<CloseSettleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_SETTLE_DISCRIMINATOR })
  );
}

export function getCloseSettleInstructionDataDecoder(): FixedSizeDecoder<CloseSettleInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseSettleInstructionDataCodec(): FixedSizeCodec<
  CloseSettleInstructionDataArgs,
  CloseSettleInstructionData
> {
  return combineCodec(
    getCloseSettleInstructionDataEncoder(),
    getCloseSettleInstructionDataDecoder()
  );
}

export type CloseSettleAsyncInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountUserVault extends string = string,
  TAccountPositionSettle extends string = string,
  TAccountMeteoraPosition extends string = string,
> = {
  /** Bot or vault.owner — dual-caller. */
  caller: TransactionSigner<TAccountCaller>;
  config?: Address<TAccountConfig>;
  userVault: Address<TAccountUserVault>;
  positionSettle?: Address<TAccountPositionSettle>;
  meteoraPosition: Address<TAccountMeteoraPosition>;
};

export async function getCloseSettleInstructionAsync<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountUserVault extends string,
  TAccountPositionSettle extends string,
  TAccountMeteoraPosition extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: CloseSettleAsyncInput<
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountPositionSettle,
    TAccountMeteoraPosition
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CloseSettleInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountPositionSettle,
    TAccountMeteoraPosition
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    userVault: { value: input.userVault ?? null, isWritable: true },
    positionSettle: { value: input.positionSettle ?? null, isWritable: true },
    meteoraPosition: {
      value: input.meteoraPosition ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }
  if (!accounts.positionSettle.value) {
    accounts.positionSettle.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([112, 111, 115, 95, 115, 101, 116, 116, 108, 101])
        ),
        getAddressEncoder().encode(
          expectAddress(accounts.meteoraPosition.value)
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.userVault),
      getAccountMeta(accounts.positionSettle),
      getAccountMeta(accounts.meteoraPosition),
    ],
    data: getCloseSettleInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseSettleInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountPositionSettle,
    TAccountMeteoraPosition
  >);
}

export type CloseSettleInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountUserVault extends string = string,
  TAccountPositionSettle extends string = string,
  TAccountMeteoraPosition extends string = string,
> = {
  /** Bot or vault.owner — dual-caller. */
  caller: TransactionSigner<TAccountCaller>;
  config: Address<TAccountConfig>;
  userVault: Address<TAccountUserVault>;
  positionSettle: Address<TAccountPositionSettle>;
  meteoraPosition: Address<TAccountMeteoraPosition>;
};

export function getCloseSettleInstruction<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountUserVault extends string,
  TAccountPositionSettle extends string,
  TAccountMeteoraPosition extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: CloseSettleInput<
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountPositionSettle,
    TAccountMeteoraPosition
  >,
  config?: { programAddress?: TProgramAddress }
): CloseSettleInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountConfig,
  TAccountUserVault,
  TAccountPositionSettle,
  TAccountMeteoraPosition
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    userVault: { value: input.userVault ?? null, isWritable: true },
    positionSettle: { value: input.positionSettle ?? null, isWritable: true },
    meteoraPosition: {
      value: input.meteoraPosition ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.userVault),
      getAccountMeta(accounts.positionSettle),
      getAccountMeta(accounts.meteoraPosition),
    ],
    data: getCloseSettleInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseSettleInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountPositionSettle,
    TAccountMeteoraPosition
  >);
}

export type ParsedCloseSettleInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Bot or vault.owner — dual-caller. */
    caller: TAccountMetas[0];
    config: TAccountMetas[1];
    userVault: TAccountMetas[2];
    positionSettle: TAccountMetas[3];
    meteoraPosition: TAccountMetas[4];
  };
  data: CloseSettleInstructionData;
};

export function parseCloseSettleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCloseSettleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      config: getNextAccount(),
      userVault: getNextAccount(),
      positionSettle: getNextAccount(),
      meteoraPosition: getNextAccount(),
    },
    data: getCloseSettleInstructionDataDecoder().decode(instruction.data),
  };
}
