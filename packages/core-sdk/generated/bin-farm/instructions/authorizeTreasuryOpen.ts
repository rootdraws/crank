/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI32Decoder,
  getI32Encoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { BIN_FARM_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const AUTHORIZE_TREASURY_OPEN_DISCRIMINATOR = new Uint8Array([
  204, 3, 120, 244, 185, 133, 44, 231,
]);

export function getAuthorizeTreasuryOpenDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    AUTHORIZE_TREASURY_OPEN_DISCRIMINATOR
  );
}

export type AuthorizeTreasuryOpenInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountUserVault extends string | AccountMeta<string> = string,
  TAccountLbPair extends string | AccountMeta<string> = string,
  TAccountTradeAuth extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountUserVault extends string
        ? ReadonlyAccount<TAccountUserVault>
        : TAccountUserVault,
      TAccountLbPair extends string
        ? ReadonlyAccount<TAccountLbPair>
        : TAccountLbPair,
      TAccountTradeAuth extends string
        ? WritableAccount<TAccountTradeAuth>
        : TAccountTradeAuth,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AuthorizeTreasuryOpenInstructionData = {
  discriminator: ReadonlyUint8Array;
  side: number;
  amount: bigint;
  minBinId: number;
  maxBinId: number;
  slippage: number;
  proposer: Address;
};

export type AuthorizeTreasuryOpenInstructionDataArgs = {
  side: number;
  amount: number | bigint;
  minBinId: number;
  maxBinId: number;
  slippage: number;
  proposer: Address;
};

export function getAuthorizeTreasuryOpenInstructionDataEncoder(): FixedSizeEncoder<AuthorizeTreasuryOpenInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['side', getU8Encoder()],
      ['amount', getU64Encoder()],
      ['minBinId', getI32Encoder()],
      ['maxBinId', getI32Encoder()],
      ['slippage', getI32Encoder()],
      ['proposer', getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: AUTHORIZE_TREASURY_OPEN_DISCRIMINATOR,
    })
  );
}

export function getAuthorizeTreasuryOpenInstructionDataDecoder(): FixedSizeDecoder<AuthorizeTreasuryOpenInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['side', getU8Decoder()],
    ['amount', getU64Decoder()],
    ['minBinId', getI32Decoder()],
    ['maxBinId', getI32Decoder()],
    ['slippage', getI32Decoder()],
    ['proposer', getAddressDecoder()],
  ]);
}

export function getAuthorizeTreasuryOpenInstructionDataCodec(): FixedSizeCodec<
  AuthorizeTreasuryOpenInstructionDataArgs,
  AuthorizeTreasuryOpenInstructionData
> {
  return combineCodec(
    getAuthorizeTreasuryOpenInstructionDataEncoder(),
    getAuthorizeTreasuryOpenInstructionDataDecoder()
  );
}

export type AuthorizeTreasuryOpenAsyncInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountUserVault extends string = string,
  TAccountLbPair extends string = string,
  TAccountTradeAuth extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * Must equal `user_vault.owner`. For the treasury vault that's NTP,
   * signable only via SPL Governance executeTransaction.
   */
  caller: TransactionSigner<TAccountCaller>;
  config?: Address<TAccountConfig>;
  userVault: Address<TAccountUserVault>;
  lbPair: Address<TAccountLbPair>;
  tradeAuth?: Address<TAccountTradeAuth>;
  systemProgram?: Address<TAccountSystemProgram>;
  side: AuthorizeTreasuryOpenInstructionDataArgs['side'];
  amount: AuthorizeTreasuryOpenInstructionDataArgs['amount'];
  minBinId: AuthorizeTreasuryOpenInstructionDataArgs['minBinId'];
  maxBinId: AuthorizeTreasuryOpenInstructionDataArgs['maxBinId'];
  slippage: AuthorizeTreasuryOpenInstructionDataArgs['slippage'];
  proposer: AuthorizeTreasuryOpenInstructionDataArgs['proposer'];
};

export async function getAuthorizeTreasuryOpenInstructionAsync<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountUserVault extends string,
  TAccountLbPair extends string,
  TAccountTradeAuth extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: AuthorizeTreasuryOpenAsyncInput<
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountLbPair,
    TAccountTradeAuth,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  AuthorizeTreasuryOpenInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountLbPair,
    TAccountTradeAuth,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    userVault: { value: input.userVault ?? null, isWritable: false },
    lbPair: { value: input.lbPair ?? null, isWritable: false },
    tradeAuth: { value: input.tradeAuth ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }
  if (!accounts.tradeAuth.value) {
    accounts.tradeAuth.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([116, 114, 97, 100, 101, 95, 97, 117, 116, 104])
        ),
        getAddressEncoder().encode(expectAddress(accounts.userVault.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.userVault),
      getAccountMeta(accounts.lbPair),
      getAccountMeta(accounts.tradeAuth),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getAuthorizeTreasuryOpenInstructionDataEncoder().encode(
      args as AuthorizeTreasuryOpenInstructionDataArgs
    ),
    programAddress,
  } as AuthorizeTreasuryOpenInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountLbPair,
    TAccountTradeAuth,
    TAccountSystemProgram
  >);
}

export type AuthorizeTreasuryOpenInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountUserVault extends string = string,
  TAccountLbPair extends string = string,
  TAccountTradeAuth extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * Must equal `user_vault.owner`. For the treasury vault that's NTP,
   * signable only via SPL Governance executeTransaction.
   */
  caller: TransactionSigner<TAccountCaller>;
  config: Address<TAccountConfig>;
  userVault: Address<TAccountUserVault>;
  lbPair: Address<TAccountLbPair>;
  tradeAuth: Address<TAccountTradeAuth>;
  systemProgram?: Address<TAccountSystemProgram>;
  side: AuthorizeTreasuryOpenInstructionDataArgs['side'];
  amount: AuthorizeTreasuryOpenInstructionDataArgs['amount'];
  minBinId: AuthorizeTreasuryOpenInstructionDataArgs['minBinId'];
  maxBinId: AuthorizeTreasuryOpenInstructionDataArgs['maxBinId'];
  slippage: AuthorizeTreasuryOpenInstructionDataArgs['slippage'];
  proposer: AuthorizeTreasuryOpenInstructionDataArgs['proposer'];
};

export function getAuthorizeTreasuryOpenInstruction<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountUserVault extends string,
  TAccountLbPair extends string,
  TAccountTradeAuth extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: AuthorizeTreasuryOpenInput<
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountLbPair,
    TAccountTradeAuth,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): AuthorizeTreasuryOpenInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountConfig,
  TAccountUserVault,
  TAccountLbPair,
  TAccountTradeAuth,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    userVault: { value: input.userVault ?? null, isWritable: false },
    lbPair: { value: input.lbPair ?? null, isWritable: false },
    tradeAuth: { value: input.tradeAuth ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.userVault),
      getAccountMeta(accounts.lbPair),
      getAccountMeta(accounts.tradeAuth),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getAuthorizeTreasuryOpenInstructionDataEncoder().encode(
      args as AuthorizeTreasuryOpenInstructionDataArgs
    ),
    programAddress,
  } as AuthorizeTreasuryOpenInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountUserVault,
    TAccountLbPair,
    TAccountTradeAuth,
    TAccountSystemProgram
  >);
}

export type ParsedAuthorizeTreasuryOpenInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Must equal `user_vault.owner`. For the treasury vault that's NTP,
     * signable only via SPL Governance executeTransaction.
     */
    caller: TAccountMetas[0];
    config: TAccountMetas[1];
    userVault: TAccountMetas[2];
    lbPair: TAccountMetas[3];
    tradeAuth: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: AuthorizeTreasuryOpenInstructionData;
};

export function parseAuthorizeTreasuryOpenInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedAuthorizeTreasuryOpenInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      config: getNextAccount(),
      userVault: getNextAccount(),
      lbPair: getNextAccount(),
      tradeAuth: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAuthorizeTreasuryOpenInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
