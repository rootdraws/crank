/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { EPOCH_VAULT_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const DRAIN_VAULT_DISCRIMINATOR = new Uint8Array([
  87, 219, 34, 249, 5, 135, 65, 116,
]);

export function getDrainVaultDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DRAIN_VAULT_DISCRIMINATOR);
}

export type DrainVaultInstruction<
  TProgram extends string = typeof EPOCH_VAULT_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountBridgeVault extends string | AccountMeta<string> = string,
  TAccountDestination extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountBridgeVault extends string
        ? WritableAccount<TAccountBridgeVault>
        : TAccountBridgeVault,
      TAccountDestination extends string
        ? WritableAccount<TAccountDestination>
        : TAccountDestination,
      ...TRemainingAccounts,
    ]
  >;

export type DrainVaultInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type DrainVaultInstructionDataArgs = { amount: number | bigint };

export function getDrainVaultInstructionDataEncoder(): FixedSizeEncoder<DrainVaultInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['amount', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: DRAIN_VAULT_DISCRIMINATOR })
  );
}

export function getDrainVaultInstructionDataDecoder(): FixedSizeDecoder<DrainVaultInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['amount', getU64Decoder()],
  ]);
}

export function getDrainVaultInstructionDataCodec(): FixedSizeCodec<
  DrainVaultInstructionDataArgs,
  DrainVaultInstructionData
> {
  return combineCodec(
    getDrainVaultInstructionDataEncoder(),
    getDrainVaultInstructionDataDecoder()
  );
}

export type DrainVaultAsyncInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountBridgeVault extends string = string,
  TAccountDestination extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config?: Address<TAccountConfig>;
  bridgeVault?: Address<TAccountBridgeVault>;
  destination: Address<TAccountDestination>;
  amount: DrainVaultInstructionDataArgs['amount'];
};

export async function getDrainVaultInstructionAsync<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TAccountBridgeVault extends string,
  TAccountDestination extends string,
  TProgramAddress extends Address = typeof EPOCH_VAULT_PROGRAM_ADDRESS,
>(
  input: DrainVaultAsyncInput<
    TAccountAuthority,
    TAccountConfig,
    TAccountBridgeVault,
    TAccountDestination
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  DrainVaultInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountBridgeVault,
    TAccountDestination
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? EPOCH_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
    bridgeVault: { value: input.bridgeVault ?? null, isWritable: true },
    destination: { value: input.destination ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            98, 114, 105, 100, 103, 101, 95, 99, 111, 110, 102, 105, 103,
          ])
        ),
      ],
    });
  }
  if (!accounts.bridgeVault.value) {
    accounts.bridgeVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            98, 114, 105, 100, 103, 101, 95, 118, 97, 117, 108, 116,
          ])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.bridgeVault),
      getAccountMeta(accounts.destination),
    ],
    data: getDrainVaultInstructionDataEncoder().encode(
      args as DrainVaultInstructionDataArgs
    ),
    programAddress,
  } as DrainVaultInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountBridgeVault,
    TAccountDestination
  >);
}

export type DrainVaultInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountBridgeVault extends string = string,
  TAccountDestination extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config: Address<TAccountConfig>;
  bridgeVault: Address<TAccountBridgeVault>;
  destination: Address<TAccountDestination>;
  amount: DrainVaultInstructionDataArgs['amount'];
};

export function getDrainVaultInstruction<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TAccountBridgeVault extends string,
  TAccountDestination extends string,
  TProgramAddress extends Address = typeof EPOCH_VAULT_PROGRAM_ADDRESS,
>(
  input: DrainVaultInput<
    TAccountAuthority,
    TAccountConfig,
    TAccountBridgeVault,
    TAccountDestination
  >,
  config?: { programAddress?: TProgramAddress }
): DrainVaultInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountConfig,
  TAccountBridgeVault,
  TAccountDestination
> {
  // Program address.
  const programAddress = config?.programAddress ?? EPOCH_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
    bridgeVault: { value: input.bridgeVault ?? null, isWritable: true },
    destination: { value: input.destination ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.bridgeVault),
      getAccountMeta(accounts.destination),
    ],
    data: getDrainVaultInstructionDataEncoder().encode(
      args as DrainVaultInstructionDataArgs
    ),
    programAddress,
  } as DrainVaultInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountBridgeVault,
    TAccountDestination
  >);
}

export type ParsedDrainVaultInstruction<
  TProgram extends string = typeof EPOCH_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    config: TAccountMetas[1];
    bridgeVault: TAccountMetas[2];
    destination: TAccountMetas[3];
  };
  data: DrainVaultInstructionData;
};

export function parseDrainVaultInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedDrainVaultInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      config: getNextAccount(),
      bridgeVault: getNextAccount(),
      destination: getNextAccount(),
    },
    data: getDrainVaultInstructionDataDecoder().decode(instruction.data),
  };
}
