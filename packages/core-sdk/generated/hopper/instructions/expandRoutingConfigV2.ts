/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { HOPPER_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const EXPAND_ROUTING_CONFIG_V2_DISCRIMINATOR = new Uint8Array([
  203, 105, 5, 214, 160, 173, 196, 198,
]);

export function getExpandRoutingConfigV2DiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    EXPAND_ROUTING_CONFIG_V2_DISCRIMINATOR
  );
}

export type ExpandRoutingConfigV2Instruction<
  TProgram extends string = typeof HOPPER_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountRoutingConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountRoutingConfig extends string
        ? WritableAccount<TAccountRoutingConfig>
        : TAccountRoutingConfig,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ExpandRoutingConfigV2InstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ExpandRoutingConfigV2InstructionDataArgs = {};

export function getExpandRoutingConfigV2InstructionDataEncoder(): FixedSizeEncoder<ExpandRoutingConfigV2InstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: EXPAND_ROUTING_CONFIG_V2_DISCRIMINATOR,
    })
  );
}

export function getExpandRoutingConfigV2InstructionDataDecoder(): FixedSizeDecoder<ExpandRoutingConfigV2InstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getExpandRoutingConfigV2InstructionDataCodec(): FixedSizeCodec<
  ExpandRoutingConfigV2InstructionDataArgs,
  ExpandRoutingConfigV2InstructionData
> {
  return combineCodec(
    getExpandRoutingConfigV2InstructionDataEncoder(),
    getExpandRoutingConfigV2InstructionDataDecoder()
  );
}

export type ExpandRoutingConfigV2AsyncInput<
  TAccountAdmin extends string = string,
  TAccountRoutingConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  /** happens in the handler since v2 layout can't deserialize a v1-sized account. */
  routingConfig?: Address<TAccountRoutingConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getExpandRoutingConfigV2InstructionAsync<
  TAccountAdmin extends string,
  TAccountRoutingConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HOPPER_PROGRAM_ADDRESS,
>(
  input: ExpandRoutingConfigV2AsyncInput<
    TAccountAdmin,
    TAccountRoutingConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ExpandRoutingConfigV2Instruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountRoutingConfig,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? HOPPER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    routingConfig: { value: input.routingConfig ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.routingConfig.value) {
    accounts.routingConfig.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            114, 111, 117, 116, 105, 110, 103, 95, 99, 111, 110, 102, 105, 103,
          ])
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.routingConfig),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getExpandRoutingConfigV2InstructionDataEncoder().encode({}),
    programAddress,
  } as ExpandRoutingConfigV2Instruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountRoutingConfig,
    TAccountSystemProgram
  >);
}

export type ExpandRoutingConfigV2Input<
  TAccountAdmin extends string = string,
  TAccountRoutingConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  /** happens in the handler since v2 layout can't deserialize a v1-sized account. */
  routingConfig: Address<TAccountRoutingConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getExpandRoutingConfigV2Instruction<
  TAccountAdmin extends string,
  TAccountRoutingConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HOPPER_PROGRAM_ADDRESS,
>(
  input: ExpandRoutingConfigV2Input<
    TAccountAdmin,
    TAccountRoutingConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): ExpandRoutingConfigV2Instruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountRoutingConfig,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? HOPPER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    routingConfig: { value: input.routingConfig ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.routingConfig),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getExpandRoutingConfigV2InstructionDataEncoder().encode({}),
    programAddress,
  } as ExpandRoutingConfigV2Instruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountRoutingConfig,
    TAccountSystemProgram
  >);
}

export type ParsedExpandRoutingConfigV2Instruction<
  TProgram extends string = typeof HOPPER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    /** happens in the handler since v2 layout can't deserialize a v1-sized account. */
    routingConfig: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: ExpandRoutingConfigV2InstructionData;
};

export function parseExpandRoutingConfigV2Instruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedExpandRoutingConfigV2Instruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      routingConfig: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getExpandRoutingConfigV2InstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
