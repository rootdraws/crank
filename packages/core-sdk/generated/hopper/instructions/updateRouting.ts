/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { HOPPER_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const UPDATE_ROUTING_DISCRIMINATOR = new Uint8Array([
  172, 129, 171, 117, 77, 70, 122, 7,
]);

export function getUpdateRoutingDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_ROUTING_DISCRIMINATOR
  );
}

export type UpdateRoutingInstruction<
  TProgram extends string = typeof HOPPER_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountRoutingConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountRoutingConfig extends string
        ? WritableAccount<TAccountRoutingConfig>
        : TAccountRoutingConfig,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateRoutingInstructionData = {
  discriminator: ReadonlyUint8Array;
  newWBuy: Option<Address>;
  newTreasury: Option<Address>;
  newPersonal: Option<Address>;
  newSolSplitBps: Option<Array<number>>;
  newSolThresholdLamports: Option<bigint>;
  newCrankerTipBps: Option<number>;
};

export type UpdateRoutingInstructionDataArgs = {
  newWBuy: OptionOrNullable<Address>;
  newTreasury: OptionOrNullable<Address>;
  newPersonal: OptionOrNullable<Address>;
  newSolSplitBps: OptionOrNullable<Array<number>>;
  newSolThresholdLamports: OptionOrNullable<number | bigint>;
  newCrankerTipBps: OptionOrNullable<number>;
};

export function getUpdateRoutingInstructionDataEncoder(): Encoder<UpdateRoutingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['newWBuy', getOptionEncoder(getAddressEncoder())],
      ['newTreasury', getOptionEncoder(getAddressEncoder())],
      ['newPersonal', getOptionEncoder(getAddressEncoder())],
      [
        'newSolSplitBps',
        getOptionEncoder(getArrayEncoder(getU16Encoder(), { size: 3 })),
      ],
      ['newSolThresholdLamports', getOptionEncoder(getU64Encoder())],
      ['newCrankerTipBps', getOptionEncoder(getU16Encoder())],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_ROUTING_DISCRIMINATOR })
  );
}

export function getUpdateRoutingInstructionDataDecoder(): Decoder<UpdateRoutingInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['newWBuy', getOptionDecoder(getAddressDecoder())],
    ['newTreasury', getOptionDecoder(getAddressDecoder())],
    ['newPersonal', getOptionDecoder(getAddressDecoder())],
    [
      'newSolSplitBps',
      getOptionDecoder(getArrayDecoder(getU16Decoder(), { size: 3 })),
    ],
    ['newSolThresholdLamports', getOptionDecoder(getU64Decoder())],
    ['newCrankerTipBps', getOptionDecoder(getU16Decoder())],
  ]);
}

export function getUpdateRoutingInstructionDataCodec(): Codec<
  UpdateRoutingInstructionDataArgs,
  UpdateRoutingInstructionData
> {
  return combineCodec(
    getUpdateRoutingInstructionDataEncoder(),
    getUpdateRoutingInstructionDataDecoder()
  );
}

export type UpdateRoutingAsyncInput<
  TAccountAdmin extends string = string,
  TAccountRoutingConfig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  routingConfig?: Address<TAccountRoutingConfig>;
  newWBuy: UpdateRoutingInstructionDataArgs['newWBuy'];
  newTreasury: UpdateRoutingInstructionDataArgs['newTreasury'];
  newPersonal: UpdateRoutingInstructionDataArgs['newPersonal'];
  newSolSplitBps: UpdateRoutingInstructionDataArgs['newSolSplitBps'];
  newSolThresholdLamports: UpdateRoutingInstructionDataArgs['newSolThresholdLamports'];
  newCrankerTipBps: UpdateRoutingInstructionDataArgs['newCrankerTipBps'];
};

export async function getUpdateRoutingInstructionAsync<
  TAccountAdmin extends string,
  TAccountRoutingConfig extends string,
  TProgramAddress extends Address = typeof HOPPER_PROGRAM_ADDRESS,
>(
  input: UpdateRoutingAsyncInput<TAccountAdmin, TAccountRoutingConfig>,
  config?: { programAddress?: TProgramAddress }
): Promise<
  UpdateRoutingInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountRoutingConfig
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? HOPPER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    routingConfig: { value: input.routingConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.routingConfig.value) {
    accounts.routingConfig.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            114, 111, 117, 116, 105, 110, 103, 95, 99, 111, 110, 102, 105, 103,
          ])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.routingConfig),
    ],
    data: getUpdateRoutingInstructionDataEncoder().encode(
      args as UpdateRoutingInstructionDataArgs
    ),
    programAddress,
  } as UpdateRoutingInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountRoutingConfig
  >);
}

export type UpdateRoutingInput<
  TAccountAdmin extends string = string,
  TAccountRoutingConfig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  routingConfig: Address<TAccountRoutingConfig>;
  newWBuy: UpdateRoutingInstructionDataArgs['newWBuy'];
  newTreasury: UpdateRoutingInstructionDataArgs['newTreasury'];
  newPersonal: UpdateRoutingInstructionDataArgs['newPersonal'];
  newSolSplitBps: UpdateRoutingInstructionDataArgs['newSolSplitBps'];
  newSolThresholdLamports: UpdateRoutingInstructionDataArgs['newSolThresholdLamports'];
  newCrankerTipBps: UpdateRoutingInstructionDataArgs['newCrankerTipBps'];
};

export function getUpdateRoutingInstruction<
  TAccountAdmin extends string,
  TAccountRoutingConfig extends string,
  TProgramAddress extends Address = typeof HOPPER_PROGRAM_ADDRESS,
>(
  input: UpdateRoutingInput<TAccountAdmin, TAccountRoutingConfig>,
  config?: { programAddress?: TProgramAddress }
): UpdateRoutingInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountRoutingConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? HOPPER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    routingConfig: { value: input.routingConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.routingConfig),
    ],
    data: getUpdateRoutingInstructionDataEncoder().encode(
      args as UpdateRoutingInstructionDataArgs
    ),
    programAddress,
  } as UpdateRoutingInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountRoutingConfig
  >);
}

export type ParsedUpdateRoutingInstruction<
  TProgram extends string = typeof HOPPER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    routingConfig: TAccountMetas[1];
  };
  data: UpdateRoutingInstructionData;
};

export function parseUpdateRoutingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateRoutingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { admin: getNextAccount(), routingConfig: getNextAccount() },
    data: getUpdateRoutingInstructionDataDecoder().decode(instruction.data),
  };
}
