/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { BANK_DISTRIBUTOR_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const NEW_EPOCH_DISCRIMINATOR = new Uint8Array([
  145, 132, 28, 115, 100, 138, 253, 96,
]);

export function getNewEpochDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(NEW_EPOCH_DISCRIMINATOR);
}

export type NewEpochInstruction<
  TProgram extends string = typeof BANK_DISTRIBUTOR_PROGRAM_ADDRESS,
  TAccountDistributor extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountDistributor extends string
        ? WritableAccount<TAccountDistributor>
        : TAccountDistributor,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountVault extends string
        ? ReadonlyAccount<TAccountVault>
        : TAccountVault,
      ...TRemainingAccounts,
    ]
  >;

export type NewEpochInstructionData = {
  discriminator: ReadonlyUint8Array;
  merkleRoot: Array<number>;
  ipfsCid: string;
};

export type NewEpochInstructionDataArgs = {
  merkleRoot: Array<number>;
  ipfsCid: string;
};

export function getNewEpochInstructionDataEncoder(): Encoder<NewEpochInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['merkleRoot', getArrayEncoder(getU8Encoder(), { size: 32 })],
      ['ipfsCid', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: NEW_EPOCH_DISCRIMINATOR })
  );
}

export function getNewEpochInstructionDataDecoder(): Decoder<NewEpochInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['merkleRoot', getArrayDecoder(getU8Decoder(), { size: 32 })],
    ['ipfsCid', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getNewEpochInstructionDataCodec(): Codec<
  NewEpochInstructionDataArgs,
  NewEpochInstructionData
> {
  return combineCodec(
    getNewEpochInstructionDataEncoder(),
    getNewEpochInstructionDataDecoder()
  );
}

export type NewEpochAsyncInput<
  TAccountDistributor extends string = string,
  TAccountAuthority extends string = string,
  TAccountVault extends string = string,
> = {
  distributor?: Address<TAccountDistributor>;
  authority: TransactionSigner<TAccountAuthority>;
  /**
   * Read-only: balance is read to compute the epoch delta. Pre-funded by
   * rover_burn_and_mint (BANK) or drain_vault+sync_native (SOL). No transfer
   * happens here.
   */
  vault: Address<TAccountVault>;
  merkleRoot: NewEpochInstructionDataArgs['merkleRoot'];
  ipfsCid: NewEpochInstructionDataArgs['ipfsCid'];
};

export async function getNewEpochInstructionAsync<
  TAccountDistributor extends string,
  TAccountAuthority extends string,
  TAccountVault extends string,
  TProgramAddress extends Address = typeof BANK_DISTRIBUTOR_PROGRAM_ADDRESS,
>(
  input: NewEpochAsyncInput<
    TAccountDistributor,
    TAccountAuthority,
    TAccountVault
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  NewEpochInstruction<
    TProgramAddress,
    TAccountDistributor,
    TAccountAuthority,
    TAccountVault
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? BANK_DISTRIBUTOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    distributor: { value: input.distributor ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.distributor.value) {
    accounts.distributor.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([100, 105, 115, 116, 114, 105, 98, 117, 116, 111, 114])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.distributor),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.vault),
    ],
    data: getNewEpochInstructionDataEncoder().encode(
      args as NewEpochInstructionDataArgs
    ),
    programAddress,
  } as NewEpochInstruction<
    TProgramAddress,
    TAccountDistributor,
    TAccountAuthority,
    TAccountVault
  >);
}

export type NewEpochInput<
  TAccountDistributor extends string = string,
  TAccountAuthority extends string = string,
  TAccountVault extends string = string,
> = {
  distributor: Address<TAccountDistributor>;
  authority: TransactionSigner<TAccountAuthority>;
  /**
   * Read-only: balance is read to compute the epoch delta. Pre-funded by
   * rover_burn_and_mint (BANK) or drain_vault+sync_native (SOL). No transfer
   * happens here.
   */
  vault: Address<TAccountVault>;
  merkleRoot: NewEpochInstructionDataArgs['merkleRoot'];
  ipfsCid: NewEpochInstructionDataArgs['ipfsCid'];
};

export function getNewEpochInstruction<
  TAccountDistributor extends string,
  TAccountAuthority extends string,
  TAccountVault extends string,
  TProgramAddress extends Address = typeof BANK_DISTRIBUTOR_PROGRAM_ADDRESS,
>(
  input: NewEpochInput<TAccountDistributor, TAccountAuthority, TAccountVault>,
  config?: { programAddress?: TProgramAddress }
): NewEpochInstruction<
  TProgramAddress,
  TAccountDistributor,
  TAccountAuthority,
  TAccountVault
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? BANK_DISTRIBUTOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    distributor: { value: input.distributor ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.distributor),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.vault),
    ],
    data: getNewEpochInstructionDataEncoder().encode(
      args as NewEpochInstructionDataArgs
    ),
    programAddress,
  } as NewEpochInstruction<
    TProgramAddress,
    TAccountDistributor,
    TAccountAuthority,
    TAccountVault
  >);
}

export type ParsedNewEpochInstruction<
  TProgram extends string = typeof BANK_DISTRIBUTOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    distributor: TAccountMetas[0];
    authority: TAccountMetas[1];
    /**
     * Read-only: balance is read to compute the epoch delta. Pre-funded by
     * rover_burn_and_mint (BANK) or drain_vault+sync_native (SOL). No transfer
     * happens here.
     */
    vault: TAccountMetas[2];
  };
  data: NewEpochInstructionData;
};

export function parseNewEpochInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedNewEpochInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      distributor: getNextAccount(),
      authority: getNextAccount(),
      vault: getNextAccount(),
    },
    data: getNewEpochInstructionDataDecoder().decode(instruction.data),
  };
}
