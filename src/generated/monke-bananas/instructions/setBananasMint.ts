/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { MONKE_BANANAS_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const SET_BANANAS_MINT_DISCRIMINATOR = new Uint8Array([
  6, 125, 29, 95, 214, 209, 152, 88,
]);

export function getSetBananasMintDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_BANANAS_MINT_DISCRIMINATOR
  );
}

export type SetBananasMintInstruction<
  TProgram extends string = typeof MONKE_BANANAS_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountState extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountState extends string
        ? WritableAccount<TAccountState>
        : TAccountState,
      ...TRemainingAccounts,
    ]
  >;

export type SetBananasMintInstructionData = {
  discriminator: ReadonlyUint8Array;
  newMint: Address;
};

export type SetBananasMintInstructionDataArgs = { newMint: Address };

export function getSetBananasMintInstructionDataEncoder(): FixedSizeEncoder<SetBananasMintInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['newMint', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_BANANAS_MINT_DISCRIMINATOR })
  );
}

export function getSetBananasMintInstructionDataDecoder(): FixedSizeDecoder<SetBananasMintInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['newMint', getAddressDecoder()],
  ]);
}

export function getSetBananasMintInstructionDataCodec(): FixedSizeCodec<
  SetBananasMintInstructionDataArgs,
  SetBananasMintInstructionData
> {
  return combineCodec(
    getSetBananasMintInstructionDataEncoder(),
    getSetBananasMintInstructionDataDecoder()
  );
}

export type SetBananasMintAsyncInput<
  TAccountAuthority extends string = string,
  TAccountState extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  state?: Address<TAccountState>;
  newMint: SetBananasMintInstructionDataArgs['newMint'];
};

export async function getSetBananasMintInstructionAsync<
  TAccountAuthority extends string,
  TAccountState extends string,
  TProgramAddress extends Address = typeof MONKE_BANANAS_PROGRAM_ADDRESS,
>(
  input: SetBananasMintAsyncInput<TAccountAuthority, TAccountState>,
  config?: { programAddress?: TProgramAddress }
): Promise<
  SetBananasMintInstruction<TProgramAddress, TAccountAuthority, TAccountState>
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MONKE_BANANAS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    state: { value: input.state ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.state.value) {
    accounts.state.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([109, 111, 110, 107, 101, 95, 115, 116, 97, 116, 101])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.state),
    ],
    data: getSetBananasMintInstructionDataEncoder().encode(
      args as SetBananasMintInstructionDataArgs
    ),
    programAddress,
  } as SetBananasMintInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountState
  >);
}

export type SetBananasMintInput<
  TAccountAuthority extends string = string,
  TAccountState extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  state: Address<TAccountState>;
  newMint: SetBananasMintInstructionDataArgs['newMint'];
};

export function getSetBananasMintInstruction<
  TAccountAuthority extends string,
  TAccountState extends string,
  TProgramAddress extends Address = typeof MONKE_BANANAS_PROGRAM_ADDRESS,
>(
  input: SetBananasMintInput<TAccountAuthority, TAccountState>,
  config?: { programAddress?: TProgramAddress }
): SetBananasMintInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountState
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MONKE_BANANAS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    state: { value: input.state ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.state),
    ],
    data: getSetBananasMintInstructionDataEncoder().encode(
      args as SetBananasMintInstructionDataArgs
    ),
    programAddress,
  } as SetBananasMintInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountState
  >);
}

export type ParsedSetBananasMintInstruction<
  TProgram extends string = typeof MONKE_BANANAS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    state: TAccountMetas[1];
  };
  data: SetBananasMintInstructionData;
};

export function parseSetBananasMintInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSetBananasMintInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), state: getNextAccount() },
    data: getSetBananasMintInstructionDataDecoder().decode(instruction.data),
  };
}
