/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { MONKE_BANANAS_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const DEPOSIT_PEGGED_DISCRIMINATOR = new Uint8Array([
  165, 123, 5, 183, 200, 77, 252, 159,
]);

export function getDepositPeggedDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DEPOSIT_PEGGED_DISCRIMINATOR
  );
}

export type DepositPeggedInstruction<
  TProgram extends string = typeof MONKE_BANANAS_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountState extends string | AccountMeta<string> = string,
  TAccountDistPool extends string | AccountMeta<string> = string,
  TAccountDistPoolPeggedAta extends string | AccountMeta<string> = string,
  TAccountProgramVaultPeggedAta extends string | AccountMeta<string> = string,
  TAccountProgramVault extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? ReadonlySignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountState extends string
        ? WritableAccount<TAccountState>
        : TAccountState,
      TAccountDistPool extends string
        ? ReadonlyAccount<TAccountDistPool>
        : TAccountDistPool,
      TAccountDistPoolPeggedAta extends string
        ? WritableAccount<TAccountDistPoolPeggedAta>
        : TAccountDistPoolPeggedAta,
      TAccountProgramVaultPeggedAta extends string
        ? WritableAccount<TAccountProgramVaultPeggedAta>
        : TAccountProgramVaultPeggedAta,
      TAccountProgramVault extends string
        ? ReadonlyAccount<TAccountProgramVault>
        : TAccountProgramVault,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type DepositPeggedInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type DepositPeggedInstructionDataArgs = {};

export function getDepositPeggedInstructionDataEncoder(): FixedSizeEncoder<DepositPeggedInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: DEPOSIT_PEGGED_DISCRIMINATOR })
  );
}

export function getDepositPeggedInstructionDataDecoder(): FixedSizeDecoder<DepositPeggedInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getDepositPeggedInstructionDataCodec(): FixedSizeCodec<
  DepositPeggedInstructionDataArgs,
  DepositPeggedInstructionData
> {
  return combineCodec(
    getDepositPeggedInstructionDataEncoder(),
    getDepositPeggedInstructionDataDecoder()
  );
}

export type DepositPeggedAsyncInput<
  TAccountCaller extends string = string,
  TAccountState extends string = string,
  TAccountDistPool extends string = string,
  TAccountDistPoolPeggedAta extends string = string,
  TAccountProgramVaultPeggedAta extends string = string,
  TAccountProgramVault extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /** Anyone can call (permissionless — keeper calls weekly) */
  caller: TransactionSigner<TAccountCaller>;
  state?: Address<TAccountState>;
  distPool?: Address<TAccountDistPool>;
  /** Dist pool's $PEGGED ATA — source (receives from bridge) */
  distPoolPeggedAta: Address<TAccountDistPoolPeggedAta>;
  /** Program vault's $PEGGED ATA — destination */
  programVaultPeggedAta: Address<TAccountProgramVaultPeggedAta>;
  programVault?: Address<TAccountProgramVault>;
  tokenProgram?: Address<TAccountTokenProgram>;
};

export async function getDepositPeggedInstructionAsync<
  TAccountCaller extends string,
  TAccountState extends string,
  TAccountDistPool extends string,
  TAccountDistPoolPeggedAta extends string,
  TAccountProgramVaultPeggedAta extends string,
  TAccountProgramVault extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof MONKE_BANANAS_PROGRAM_ADDRESS,
>(
  input: DepositPeggedAsyncInput<
    TAccountCaller,
    TAccountState,
    TAccountDistPool,
    TAccountDistPoolPeggedAta,
    TAccountProgramVaultPeggedAta,
    TAccountProgramVault,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  DepositPeggedInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountState,
    TAccountDistPool,
    TAccountDistPoolPeggedAta,
    TAccountProgramVaultPeggedAta,
    TAccountProgramVault,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MONKE_BANANAS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    state: { value: input.state ?? null, isWritable: true },
    distPool: { value: input.distPool ?? null, isWritable: false },
    distPoolPeggedAta: {
      value: input.distPoolPeggedAta ?? null,
      isWritable: true,
    },
    programVaultPeggedAta: {
      value: input.programVaultPeggedAta ?? null,
      isWritable: true,
    },
    programVault: { value: input.programVault ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.state.value) {
    accounts.state.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([109, 111, 110, 107, 101, 95, 115, 116, 97, 116, 101])
        ),
      ],
    });
  }
  if (!accounts.distPool.value) {
    accounts.distPool.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([100, 105, 115, 116, 95, 112, 111, 111, 108])
        ),
      ],
    });
  }
  if (!accounts.programVault.value) {
    accounts.programVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            112, 114, 111, 103, 114, 97, 109, 95, 118, 97, 117, 108, 116,
          ])
        ),
      ],
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA' as Address<'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.state),
      getAccountMeta(accounts.distPool),
      getAccountMeta(accounts.distPoolPeggedAta),
      getAccountMeta(accounts.programVaultPeggedAta),
      getAccountMeta(accounts.programVault),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getDepositPeggedInstructionDataEncoder().encode({}),
    programAddress,
  } as DepositPeggedInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountState,
    TAccountDistPool,
    TAccountDistPoolPeggedAta,
    TAccountProgramVaultPeggedAta,
    TAccountProgramVault,
    TAccountTokenProgram
  >);
}

export type DepositPeggedInput<
  TAccountCaller extends string = string,
  TAccountState extends string = string,
  TAccountDistPool extends string = string,
  TAccountDistPoolPeggedAta extends string = string,
  TAccountProgramVaultPeggedAta extends string = string,
  TAccountProgramVault extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /** Anyone can call (permissionless — keeper calls weekly) */
  caller: TransactionSigner<TAccountCaller>;
  state: Address<TAccountState>;
  distPool: Address<TAccountDistPool>;
  /** Dist pool's $PEGGED ATA — source (receives from bridge) */
  distPoolPeggedAta: Address<TAccountDistPoolPeggedAta>;
  /** Program vault's $PEGGED ATA — destination */
  programVaultPeggedAta: Address<TAccountProgramVaultPeggedAta>;
  programVault: Address<TAccountProgramVault>;
  tokenProgram?: Address<TAccountTokenProgram>;
};

export function getDepositPeggedInstruction<
  TAccountCaller extends string,
  TAccountState extends string,
  TAccountDistPool extends string,
  TAccountDistPoolPeggedAta extends string,
  TAccountProgramVaultPeggedAta extends string,
  TAccountProgramVault extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof MONKE_BANANAS_PROGRAM_ADDRESS,
>(
  input: DepositPeggedInput<
    TAccountCaller,
    TAccountState,
    TAccountDistPool,
    TAccountDistPoolPeggedAta,
    TAccountProgramVaultPeggedAta,
    TAccountProgramVault,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): DepositPeggedInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountState,
  TAccountDistPool,
  TAccountDistPoolPeggedAta,
  TAccountProgramVaultPeggedAta,
  TAccountProgramVault,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MONKE_BANANAS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    state: { value: input.state ?? null, isWritable: true },
    distPool: { value: input.distPool ?? null, isWritable: false },
    distPoolPeggedAta: {
      value: input.distPoolPeggedAta ?? null,
      isWritable: true,
    },
    programVaultPeggedAta: {
      value: input.programVaultPeggedAta ?? null,
      isWritable: true,
    },
    programVault: { value: input.programVault ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA' as Address<'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.state),
      getAccountMeta(accounts.distPool),
      getAccountMeta(accounts.distPoolPeggedAta),
      getAccountMeta(accounts.programVaultPeggedAta),
      getAccountMeta(accounts.programVault),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getDepositPeggedInstructionDataEncoder().encode({}),
    programAddress,
  } as DepositPeggedInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountState,
    TAccountDistPool,
    TAccountDistPoolPeggedAta,
    TAccountProgramVaultPeggedAta,
    TAccountProgramVault,
    TAccountTokenProgram
  >);
}

export type ParsedDepositPeggedInstruction<
  TProgram extends string = typeof MONKE_BANANAS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Anyone can call (permissionless — keeper calls weekly) */
    caller: TAccountMetas[0];
    state: TAccountMetas[1];
    distPool: TAccountMetas[2];
    /** Dist pool's $PEGGED ATA — source (receives from bridge) */
    distPoolPeggedAta: TAccountMetas[3];
    /** Program vault's $PEGGED ATA — destination */
    programVaultPeggedAta: TAccountMetas[4];
    programVault: TAccountMetas[5];
    tokenProgram: TAccountMetas[6];
  };
  data: DepositPeggedInstructionData;
};

export function parseDepositPeggedInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedDepositPeggedInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      state: getNextAccount(),
      distPool: getNextAccount(),
      distPoolPeggedAta: getNextAccount(),
      programVaultPeggedAta: getNextAccount(),
      programVault: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getDepositPeggedInstructionDataDecoder().decode(instruction.data),
  };
}
