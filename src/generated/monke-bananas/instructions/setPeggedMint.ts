/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { MONKE_BANANAS_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const SET_PEGGED_MINT_DISCRIMINATOR = new Uint8Array([
  2, 222, 230, 177, 221, 229, 34, 66,
]);

export function getSetPeggedMintDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_PEGGED_MINT_DISCRIMINATOR
  );
}

export type SetPeggedMintInstruction<
  TProgram extends string = typeof MONKE_BANANAS_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountState extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountState extends string
        ? WritableAccount<TAccountState>
        : TAccountState,
      ...TRemainingAccounts,
    ]
  >;

export type SetPeggedMintInstructionData = {
  discriminator: ReadonlyUint8Array;
  peggedMint: Address;
};

export type SetPeggedMintInstructionDataArgs = { peggedMint: Address };

export function getSetPeggedMintInstructionDataEncoder(): FixedSizeEncoder<SetPeggedMintInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['peggedMint', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_PEGGED_MINT_DISCRIMINATOR })
  );
}

export function getSetPeggedMintInstructionDataDecoder(): FixedSizeDecoder<SetPeggedMintInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['peggedMint', getAddressDecoder()],
  ]);
}

export function getSetPeggedMintInstructionDataCodec(): FixedSizeCodec<
  SetPeggedMintInstructionDataArgs,
  SetPeggedMintInstructionData
> {
  return combineCodec(
    getSetPeggedMintInstructionDataEncoder(),
    getSetPeggedMintInstructionDataDecoder()
  );
}

export type SetPeggedMintAsyncInput<
  TAccountAuthority extends string = string,
  TAccountState extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  state?: Address<TAccountState>;
  peggedMint: SetPeggedMintInstructionDataArgs['peggedMint'];
};

export async function getSetPeggedMintInstructionAsync<
  TAccountAuthority extends string,
  TAccountState extends string,
  TProgramAddress extends Address = typeof MONKE_BANANAS_PROGRAM_ADDRESS,
>(
  input: SetPeggedMintAsyncInput<TAccountAuthority, TAccountState>,
  config?: { programAddress?: TProgramAddress }
): Promise<
  SetPeggedMintInstruction<TProgramAddress, TAccountAuthority, TAccountState>
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MONKE_BANANAS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    state: { value: input.state ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.state.value) {
    accounts.state.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([109, 111, 110, 107, 101, 95, 115, 116, 97, 116, 101])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.state),
    ],
    data: getSetPeggedMintInstructionDataEncoder().encode(
      args as SetPeggedMintInstructionDataArgs
    ),
    programAddress,
  } as SetPeggedMintInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountState
  >);
}

export type SetPeggedMintInput<
  TAccountAuthority extends string = string,
  TAccountState extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  state: Address<TAccountState>;
  peggedMint: SetPeggedMintInstructionDataArgs['peggedMint'];
};

export function getSetPeggedMintInstruction<
  TAccountAuthority extends string,
  TAccountState extends string,
  TProgramAddress extends Address = typeof MONKE_BANANAS_PROGRAM_ADDRESS,
>(
  input: SetPeggedMintInput<TAccountAuthority, TAccountState>,
  config?: { programAddress?: TProgramAddress }
): SetPeggedMintInstruction<TProgramAddress, TAccountAuthority, TAccountState> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MONKE_BANANAS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    state: { value: input.state ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.state),
    ],
    data: getSetPeggedMintInstructionDataEncoder().encode(
      args as SetPeggedMintInstructionDataArgs
    ),
    programAddress,
  } as SetPeggedMintInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountState
  >);
}

export type ParsedSetPeggedMintInstruction<
  TProgram extends string = typeof MONKE_BANANAS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    state: TAccountMetas[1];
  };
  data: SetPeggedMintInstructionData;
};

export function parseSetPeggedMintInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSetPeggedMintInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), state: getNextAccount() },
    data: getSetPeggedMintInstructionDataDecoder().decode(instruction.data),
  };
}
